# HIS-Mixed 项目进度追踪

> 最后更新：2026年6月3日（细化 §10.3 / §10.4 / §10.9 待办子项）  
> 项目阶段：**可演示验收阶段**（浏览器双端 H5 已通过冒烟；微信壳待验收；**企业级生产就绪见 §10.9**）  
> 整体代码完成度：**约 98%** | **可演示就绪度：约 96%** | **企业级就绪度：约 55%**（MVP 可演示，生产/安全/CI 待建设）  
> **全部待办勾选清单**：§十（演示 **P0～P3** + 企业级 **ENT-***）  
> 最新提交：`3ebb595`（main，与 origin 同步）

---

## 一、整体进度概览

| 维度 | 完成度 | 状态 | 说明 |
|------|--------|------|------|
| **项目文档** | 90% | ✅ 完整 | README、架构设计、技术选型、API规范、数据库设计文档齐全 |
| **数据库迁移** | 95% | ✅ 完整 | 18个迁移脚本覆盖17个数据库，种子数据就绪 |
| **Go 公共库** | 75% | ✅ 可用 | JWT、错误码、响应封装、中间件等模块可编译运行，JWT已支持HMAC-SHA |
| **Go 网关** | 70% | ✅ 可用 | JWT HMAC 互通；前端兼容路由（registration/list、billing/bill-items、pharmacy/prescriptions 等）；`/api/health` |
| **Go 业务服务** | 75% | ✅ 大部分完成 | 8个服务已实现完整业务逻辑，Statistics服务补充真实统计查询 |
| **Proto 接口定义** | 95% | ✅ 完整 | 18个proto已编写，Go和Java代码已生成 |
| **Java 服务** | 75% | ✅ 大部分完成 | Auth/User/Clinic/EMR/Prescription/Billing/Inpatient/HealthRecord/System均已有业务代码，Auth和User已启动验证 |
| **前端患者端（H5）** | 90% | ✅ build成功 | `his-web-patient`：10 页已接 API；首页已绑登录态；挂号科室已改调API |
| **前端管理端（Web）** | 95% | ✅ build成功 | `his-web-admin`：8 页已接 API；`npm run build` 验证通过 |
| **可演示就绪度** | 96% | ✅ 浏览器通过 | P0 冒烟完成；Gateway 前端路由与健康检查已补；微信壳 §10.4 待验收 |
| **Docker 基础设施** | 85% | ✅ 配置完整 | PostgreSQL、Redis、RabbitMQ、Nacos、MinIO配置就绪 |
| **Docker 服务编排** | 85% | ✅ 可用 | `start_demo.sh` 一键启 infra+DB+最小后端+双前端；`FULL_MODE=true` 启全量服务 |
| **K8s 部署** | 60% | 🔶 有代码 | YAML结构完整，镜像未实际构建 |
| **单元测试** | 45% | 🔶 有覆盖 | Go 约 70+ 用例（pkg + 8 个业务服务 service 层）；Java 无 |
| **集成测试** | 15% | 🔶 基础 | 仅4个Gateway级用例 |
| **企业级就绪度** | 55% | 🔶 待建设 | 无 CI/CD；Java 零测试；7/9 Java 服务缺 SecurityConfig；密钥硬编码；Gateway 路径未统一 — 见 **§10.9** |
| **辅助脚本** | 95% | ✅ 完整 | `start_demo.sh`、`verify_demo.sh`、DB 初始化、proto 生成就绪 |

---

## 二、各模块详细状态

### 2.1 后端Go服务（9个服务）

| 服务 | 入口 | 内部实现 | 配置 | 测试 | 完成度 |
|------|------|----------|------|------|--------|
| **Gateway** | ✅ 完整 | ✅ 路由+HTTP代理+兼容路由 | ✅ 有 | 🔶 中间件测试 | 70% |
| Registration | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 75% |
| Pharmacy | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 75% |
| Examination | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 75% |
| Schedule | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 75% |
| Outpatient | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 78% |
| Followup | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 75% |
| Notification | ✅ 完整 | ✅ 完整业务逻辑 | ✅ 有 | ✅ service 测试 | 82% |
| Statistics | ✅ 完整 | ✅ 真实统计逻辑 | ✅ 有 | ✅ service 测试 | 70% |

**已解决问题：**
- 8个服务（Registration/Schedule/Pharmacy/Examination/Outpatient/Followup/Notification/Statistics）已实现完整业务逻辑
- 所有服务已补充配置文件
- 所有服务main.go已补充初始化逻辑（database.Connect()、redis.Connect()等）
- gRPC Service已注册实现
- Proto代码已生成（Go和Java）
- Statistics服务已补充真实SQL统计查询（挂号/门诊/药房统计）
- Notification服务GetTemplates接口已对接数据库
- Gateway HMAC JWT、Registration/Schedule/Statistics/Pharmacy HTTP 端点、`/departments/tree` 路由（2026-06-03）
- Gateway 前端兼容路由：`/registration/list|register|/:id/cancel`、`/billing/bill-items`、`/pharmacy/prescriptions` 等（`3ebb595`）

**剩余问题：**
- Statistics服务趋势数据查询需进一步优化
- 缺少集成测试与端到端测试

### 2.2 后端Java服务（9个服务）

| 服务 | 启动类 | 配置 | 业务代码 | 完成度 |
|------|--------|------|----------|--------|
| **his-auth** | ✅ 有 | ✅ 有 | ✅ 有Entity/Mapper/Service/Controller + JWT过滤器 | 75% |
| his-user | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 80% |
| his-clinic | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 85% |
| his-emr | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 80% |
| his-prescription | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 80% |
| his-billing | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 75% |
| his-inpatient | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 75% |
| his-health-record | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 75% |
| his-system | ✅ 有 | ✅ 有 | ✅ 有Entity/Repository/Service/Controller | 75% |

**已解决问题：**
- his-auth服务已实现完整业务逻辑（登录、注册、用户查询）
- his-user服务已实现患者档案、员工管理、科室管理
- his-user 已添加 SecurityConfig，公开 departments/patients 端点；ORM 统一为 Spring Data JPA（2026-06-03）
- his-auth `/api/auth/health`、his-user `/api/patients/health` 健康检查端点（`3ebb595`）
- his-clinic服务已实现接诊登记、诊断录入、转诊管理
- his-emr服务已实现SOAP结构化病历管理
- his-prescription服务已实现处方开具、审核、主从表设计
- his-billing服务已实现费用计算、收费退费、支付管理
- his-inpatient服务已实现入院登记、出院管理、床位管理
- his-health-record服务已实现健康档案、健康时间线
- his-system服务已实现字典管理、审计日志

**剩余问题：**
- 无gRPC Service实现类（仅REST API）
- 无Java侧单元测试
- 无Seata分布式事务配置

### 2.3 Proto接口定义（18个）

| 项目 | 状态 | 说明 |
|------|------|------|
| 文件数量 | ✅ 完整 | 18个目录/文件齐全 |
| 接口定义 | 🔶 有代码 | 大部分proto仅定义2-3个RPC方法，覆盖面较窄 |
| 代码生成 | ✅ 完成 | `.pb.go`和Java stub均已生成 |

### 2.4 前端（可演示目标）

| 项目 | 路径 | 端口 | 状态 | 说明 |
|------|------|------|------|------|
| **管理端 Web** | `frontend/his-web-admin` | 5175 | ✅ build 通过 | Vue3 + TS + Ant Design Vue，`base: /admin`；8 页已接 API |
| **患者端 H5** | `frontend/his-web-patient` | 5174 | ✅ build 通过 | 10 页已接 API；首页登录态、挂号科室 API 已对接 |
| **微信开发者工具壳（患者端）** | `frontend/his-mp-webview` | — | ✅ 功能完整 | **仅服务患者端**：web-view + 加载/失败兜底；详见 **§9.7** |

**患者端（his-web-patient）完成情况：**
- ✅ 已对接 API 的页面：Login、Registration、MyPrescriptions、MyAppointments、MyBills、MyReports、MyFollowup、Consultation、Profile
- ✅ API 模块：auth、schedule、registration、pharmacy、billing、report、followup、consultation、**department**
- ✅ 路由守卫：`requiresAuth` + `localStorage.token`
- ✅ Vite 代理：`/api` → `http://localhost:8080`
- ✅ `Home.vue` 已绑定 `authStore`（问候语 + 登录昵称）
- ✅ `Registration.vue` 科室列表改调 `departmentApi`（Gateway → his-user `/departments`）
- ✅ 浏览器冒烟：登录 → 挂号 → 我的挂号 → 个人中心（P0-23～P0-26，2026-06-03）

**管理端（his-web-admin）完成情况：**
- ✅ 已对接 API 的页面：Login、Dashboard、DoctorList、ScheduleList、DrugList、PatientList、DepartmentList、UserList
- ✅ API 模块：auth、doctor、schedule、drug、department、statistics、patient
- ✅ 路由守卫 + Pinia auth
- ✅ `npm run build` 已在提交 `8d9fb80` 验证通过
- ✅ 浏览器冒烟：工作台/科室树/排班/药品/患者列表（P0-28～P0-33，2026-06-03）
- 🔶 **扩展页**：处方/费用等需 `FULL_MODE=true` 启动对应 Java 服务

### 2.5 基础设施与部署

| 项目 | 状态 | 说明 |
|------|------|------|
| Docker基础设施 | ✅ 完整 | PostgreSQL、Redis、RabbitMQ、Nacos、MinIO配置就绪 |
| Docker服务编排 | ✅ 可用 | `scripts/start_demo.sh` 自动启最小后端+双前端；`scripts/verify_demo.sh` 健康检查；`FULL_MODE=true` 启 pharmacy 等 |
| K8s部署 | 🔶 有代码 | 12个YAML清单，镜像未实际构建 |
| Nginx配置 | ✅ 完整 | 前端静态资源 + /api/ 反向代理 |

### 2.6 测试

| 类型 | 数量 | 覆盖范围 | 状态 |
|------|------|----------|------|
| Go单元测试（pkg） | 约21个 | JWT、错误码、响应、中间件、通用工具 | ✅ 通过 |
| Go业务层测试（service） | 约50+个 | Registration/Statistics/Pharmacy/Schedule/Examination/Outpatient/Followup/Notification | ✅ 通过 |
| Java单元测试 | 0个 | 无 | ❌ 缺失 |
| 集成测试 | 4个 | Gateway健康检查、认证流程 | 🔶 基础 |

---

## 三、已完成的工作

### 3.1 基础架构搭建（完成度：90%）
- [x] 项目目录结构规划与创建
- [x] 18个微服务骨架（Go 9个 + Java 9个）
- [x] 18个Proto接口定义文件
- [x] Go公共库12个模块（JWT、错误码、响应、中间件等）
- [x] Java父POM依赖管理
- [x] 数据库18个迁移脚本
- [x] 种子数据（管理员账号、角色、字典数据）
- [x] Proto代码生成（Go和Java）
- [x] Go服务配置文件补全（9个服务全部有配置文件）
- [x] Go服务main.go初始化逻辑补全（9个服务全部有完整初始化）
- [x] Dockerfile多服务构建问题修复（Go和Java）

### 3.2 业务服务实现（完成度：75%）
- [x] Registration服务完整业务逻辑（挂号、退号、排队叫号）
- [x] Schedule服务完整业务逻辑（排班计划、时段管理）
- [x] Pharmacy服务完整业务逻辑（库存校验、发药）
- [x] Examination服务完整业务逻辑（检查申请、报告）
- [x] Auth服务完整业务逻辑（登录、注册、用户查询）
- [x] Outpatient服务完整业务逻辑（在线问诊、会诊管理）
- [x] Followup服务完整业务逻辑（随访计划、随访记录）
- [x] Notification服务完整业务逻辑（消息发送、模板管理）
- [x] Statistics服务真实统计逻辑（挂号/门诊/药房统计、趋势查询）
- [x] his-user服务完整业务逻辑（患者档案、员工管理、科室管理）
- [x] his-clinic服务完整业务逻辑（接诊登记、诊断录入、转诊管理）
- [x] his-emr服务完整业务逻辑（SOAP结构化病历管理）
- [x] his-prescription服务完整业务逻辑（处方开具、审核）
- [x] his-billing服务完整业务逻辑（费用计算、收费退费）
- [x] his-inpatient服务完整业务逻辑（入院登记、出院管理）
- [x] his-health-record服务完整业务逻辑（健康档案、健康时间线）
- [x] his-system服务完整业务逻辑（字典管理、审计日志）

### 3.2 部署配置（完成度：80%）
- [x] Docker Compose基础设施编排
- [x] Docker Compose服务编排（Go + Java）
- [x] Dockerfile多阶段构建（Go + Java）
- [x] K8s部署清单（12个YAML）
- [x] Nginx反向代理配置
- [x] 环境变量模板（.env.example）

### 3.3 文档体系（完成度：90%）
- [x] README.md（677行，极为详尽）
- [x] 项目架构设计文档
- [x] 技术选型文档
- [x] API接口规范文档
- [x] 数据库设计文档
- [x] 混合架构规划文档

### 3.4 工具链（完成度：90%）
- [x] Go代码质量检查脚本（check.sh）
- [x] Proto代码生成脚本（gen-go.sh、gen-java.sh）
- [x] 数据库初始化脚本（db_init.sh）
- [x] 集成测试运行脚本（run.sh）

### 3.5 前端实现（完成度：85%）
**患者端（his-web-patient）：**
- [x] 项目骨架搭建（Vue3 + TypeScript + Ant Design Vue + Vite）
- [x] 登录/注册页面对接Auth服务
- [x] 在线挂号页面对接Registration服务
- [x] 我的处方页面对接Pharmacy服务
- [x] 我的挂号页面对接Registration服务
- [x] 费用查询页面对接Billing服务
- [x] 检查报告页面对接Examination服务
- [x] 我的随访页面对接Followup服务
- [x] 在线问诊页面对接Outpatient服务
- [x] 个人中心页面连接authStore
- [x] 路由守卫登录检查

**管理端（his-web-admin）：**
- [x] 项目骨架搭建（Vue3 + TypeScript + Ant Design Vue + Vite）
- [x] 登录页面对接Auth服务
- [x] 医生管理页面对接Doctor服务
- [x] 排班管理页面对接Schedule服务
- [x] 药品管理页面对接Drug服务
- [x] 科室管理页面对接Department服务
- [x] 用户管理页面对接Auth服务
- [x] 路由守卫和权限控制

---

## 四、待完成的工作

> 历史任务记录见下文；**当前未完成项的统一勾选清单**见 **§十**。

### 4.1 高优先级（第一阶段核心）✅ 已完成
- [x] **修复Dockerfile多服务构建问题**
  - Go：使用构建参数SERVICE_NAME指定服务名
  - Java：使用构建参数SERVICE_NAME指定服务名
- [x] **生成Proto代码**
  - 运行`scripts/proto-gen/gen-go.sh`生成`.pb.go`
  - 运行`scripts/proto-gen/gen-java.sh`生成Java stub
- [x] **实现核心服务完整链路**
  - Registration服务完整实现（挂号、退号、排队叫号）
  - Schedule服务完整实现（排班计划、时段管理）
  - Pharmacy服务完整实现（库存校验、发药）
  - Examination服务完整实现（检查申请、报告）
  - Auth服务完整实现（登录、注册、用户查询）
- [x] **补充Go服务main.go初始化逻辑**
  - 数据库连接（database.Connect()）
  - Redis连接（redis.Connect()）
  - 配置加载（config.Load()）
  - gRPC Service注册
- [x] **补充Go服务配置文件**
  - 为所有9个Go服务添加YAML配置文件

### 4.2 高优先级（可演示验收 — **浏览器双端已完成**）

目标：**Web 管理端** + **患者端 H5** 完成端到端演示。

| 序号 | 任务 | 优先级 | 状态 |
|------|------|--------|------|
| 1 | 定义并文档化**最小演示后端集**（见 `docs/演示说明.md` §1.3 与 §9.1） | P0 | ✅ |
| 2 | 修复并补全 `scripts/start_demo.sh`（infra+DB+后端+前端） | P0 | ✅ |
| 3 | 演示数据：拆分 SQL + `db_init.sh` 按库灌数 | P0 | ✅ |
| 4 | 管理端：`npm run build` + 演示路径冒烟 | P0 | ✅ P0-28～P0-33 |
| 5 | 患者端：`Home.vue` + `Registration.vue` 科室 API | P1 | ✅ |
| 6 | 患者端核心链路联调（登录→挂号→我的挂号→个人中心） | P0 | ✅ P0-23～P0-26 |
| 7 | 同步 `docs/演示说明.md`（微信壳、服务集、SQL 口径） | P1 | ✅ 2026-06-03 已同步 |
| 8 | 患者端微信演示：`his-mp-webview`（§9.7 / §10.4） | P2 | 🔶 壳就绪；D1～D7 待勾选 |
| 9 | Gateway 前端兼容路由 + 核心健康检查 | P1 | ✅ `3ebb595` |

**已完成（不再阻塞骨架）：**
- [x] 管理端项目 `frontend/his-web-admin` 与 8 个业务页面 API 对接
- [x] 患者端 10 个页面 API 对接与路由守卫
- [x] `docs/演示说明.md`、`backend/go/sql/demo_data.sql` 初版

### 4.3 低优先级（第二阶段及以后）

> **企业级生产对齐**（安全、CI/CD、可观测性、K8s 生产化等）见 **§10.9**，与下列条目部分重叠，以 §10.9 勾选为准。

- [ ] **Nacos Go SDK集成**
  - 服务注册与发现
  - 动态配置加载
- [ ] **RabbitMQ Consumer实现**
  - 消息消费端逻辑
  - 幂等性保障
- [ ] **Seata分布式事务配置**
  - Java侧AT模式配置
- [ ] **完善测试覆盖**
  - [x] Go业务层单元测试（Registration/Statistics/Pharmacy）— 2026-06-02 已补充
  - [x] Go业务层单元测试（Schedule/Examination/Outpatient/Followup/Notification）— 2026-06-02 已补充
  - Java单元测试
  - 业务链路集成测试
- [ ] **修复Docker端口映射不一致**
  - 统一README与docker-compose端口定义
- [ ] **K8s镜像构建与测试**
  - 构建实际镜像并验证部署

---

## 五、关键问题与风险

### 5.1 架构/设计问题
| 问题 | 影响 | 解决方案 | 状态 |
|------|------|----------|------|
| Go Dockerfile无法支持多服务 | 无法正确运行多个Go服务 | 为每个服务单独构建或使用符号链接/命令分发 | ✅ 已修复 |
| Java Dockerfile的jar覆盖 | 服务互相覆盖无法区分 | 为每个服务单独构建或使用不同文件名 | ✅ 已修复 |
| 端口映射不一致 | 部署配置混乱 | 统一README与docker-compose端口定义 | 🔶 见 P1-13 |
| Go服务未初始化基础设施 | 服务无法连接数据库/Redis/MQ | 补充main.go初始化逻辑 | ✅ 已修复 |
| Go服务gRPC未注册实现 | 无法处理gRPC请求 | 实现并注册gRPC Service | ✅ 已修复 |

### 5.2 缺失的关键组件

> 企业级补齐计划见 **§10.9**（ENT-P1-11～12、ENT-P2-06 等）。

| 组件 | 重要性 | 说明 |
|------|--------|------|
| Nacos Go SDK集成 | 中 | README提到但代码中未实现 |
| RabbitMQ Consumer | 中 | 仅有Publisher封装，无Consumer端 |

### 5.3 技术债务
- [x] Go服务main.go缺少优雅关闭（graceful shutdown）— 2026-06-02 已修复
- [x] RequestID生成碰撞Bug — 2026-06-02 已修复（改用crypto/rand）
- [x] Gateway配置未加载（地址硬编码）— 2026-06-02 已修复（viper加载YAML）
- [x] Statistics GetTrendData类型断言panic — 2026-06-02 已修复（统一float64）
- [x] Java AuthService refreshToken Bug — 2026-06-02 已修复（绕过密码验证）
- [x] Gateway RateLimiter已实现未启用 — 2026-06-02 已启用
- 🔶 健康检查：Gateway `/api/health`、Go 各服务 `/health`、his-auth/his-user 已补；其余 Java/Go 服务待统一（见 P1-12）
- 缺少配置热更新机制
- 缺少日志级别动态调整
- 缺少链路追踪集成（OpenTelemetry）

### 5.4 已知缺陷
| 缺陷 | 影响 | 严重度 | 状态 | 说明 |
|------|------|--------|------|------|
| ~~Notification服务proto编译失败~~ | ~~notification服务无法编译运行~~ | ~~高~~ | ✅ 已修复 | `pb.PageResponse`改为`common.PageResponse` |
| ~~Statistics趋势查询忽略粒度参数~~ | ~~前端请求月度数据仍返回日度~~ | ~~中~~ | ✅ 已修复 | 新增`trendDateExpr()`支持日/周/月粒度 |
| ~~Statistics dept_stats始终返回空~~ | ~~仪表盘科室维度统计缺失~~ | ~~中~~ | ✅ 已修复 | 添加encounters按department_id分组统计 |
| ~~Statistics total_inpatients始终为0~~ | ~~住院统计缺失~~ | ~~低~~ | ✅ 已修复 | 添加inpatient_admissions表COUNT查询 |
| ~~Java his-auth JWT过滤器缺失~~ | ~~认证链路不完整~~ | ~~高~~ | ✅ 已修复 | 新建JwtAuthenticationFilter，注册到SecurityConfig |
| ~~Go服务编译产物未清理~~ | ~~仓库体积膨胀~~ | ~~低~~ | ✅ 已修复 | 删除9个ELF二进制，.gitignore已覆盖 |
| Go Gateway缺少熔断器 | 下游故障时持续发送请求 | 中 | 待修复 | 无Circuit Breaker机制 |
| Go Gateway缺少负载均衡 | 无法支持多实例部署 | 中 | 待修复 | 每个服务只有一个硬编码地址 |
| Java ORM框架不一致 | 维护成本增加 | 低 | 待修复 | his-auth用MyBatis-Plus，其余8个用Spring Data JPA |

---

## 六、下一阶段计划

### 当前冲刺：P1 体验完善 + 微信壳验收

> 浏览器双端 P0 ✅ 已完成；当前聚焦 P1 扩展页与 **§10.4 微信壳**。

| 任务 | 预计工时 | 状态 | §十 编号 |
|------|----------|------|----------|
| Gateway 前端兼容路由 + 健康检查 | 0.5 天 | ✅ | P1-12 部分 |
| 双端 `npm run build` 复检 | 0.25 天 | ✅ | P1-05 |
| 扩展页（处方/费用/报告）联调 | 1 天 | 🔶 | P1-01～P1-03 |
| 微信壳模拟器验收 | 0.5 天 | 🔶 **当前** | P2-D1～D7 |

### 第一阶段：核心服务实现
**状态**：✅ 已完成（见第三节、第八节 MVP）

### 第二阶段：院外患者服务
**目标**：完善在线问诊、慢病管理、随访服务

### 第三阶段：医保与支付
**目标**：对接医保接口、第三方支付

### 第四阶段：分布式事务
**目标**：引入分布式事务方案（跨语言DTM或SAGA模式）

### 第五阶段：容器编排迁移
**目标**：Kubernetes容器编排迁移，CI/CD流水线搭建

### 第六阶段：监控体系
**目标**：接入OpenTelemetry全链路追踪 + Prometheus + Grafana监控体系

---

## 七、进度更新记录

| 日期 | 更新内容 | 备注 |
|------|----------|------|
| 2026-06-01 | 初始版本创建 | 基于项目全面分析 |
| 2026-06-01 | 添加MVP演示版本里程碑 | 以基本功能可演示为目标，规划6周实施计划 |
| 2026-06-01 | 完成第一阶段核心任务 | 修复Dockerfile、生成Proto代码、实现4个核心Go服务和Auth服务 |
| 2026-06-01 | 完成第二阶段前端患者端API对接 | 创建API接口、Pinia store、对接登录/挂号/处方页面 |
| 2026-06-02 | 完成第三阶段前端管理端搭建 | 创建Vue3+TS+Ant Design Vue项目，实现登录、医生管理、排班管理、药品管理、患者管理、科室管理、用户管理页面 |
| 2026-06-02 | 完成第四阶段演示准备 | 创建演示数据脚本、编写演示说明文档，MVP里程碑全部完成 |
| 2026-06-02 | 完成第五阶段业务服务完善 | 实现剩余4个Go服务业务逻辑、实现8个Java服务业务代码、完善Statistics服务统计查询 |
| 2026-06-02 | 完成第六阶段前端完善 | 完善患者端7个页面API对接、添加4个API模块、实现路由守卫登录检查、修复Profile页面状态管理 |
| 2026-06-02 | 完成第七阶段管理端完善 | 完善Dashboard统计数据对接、完善PatientList患者管理对接、添加statistics和patient API模块 |
| 2026-06-02 | 完成第八阶段Bug修复与加固 | 修复RequestID碰撞、Statistics panic、Auth刷新Bug；添加优雅关闭、Gateway配置加载、RateLimiter启用；补充Registration/Statistics/Pharmacy单元测试 |
| 2026-06-02 | 修复6项已知缺陷 | Notification编译修复、Statistics粒度/dept_stats/inpatients修复、Java JWT过滤器、清理编译产物 |
| 2026-06-02 | 补充Go业务层单元测试 | 新增Schedule/Examination/Outpatient/Followup/Notification服务单元测试，修复Notification repository字段名Bug |
| 2026-06-02 | 可演示验收状态核查 | 对照仓库实测：前端双端已建、MVP 代码完成；演示脚本/患者账号/挂号硬编码等待收口 |
| 2026-06-02 | P2 澄清：仅需微信开发者工具演示 | 新增 `frontend/his-mp-webview`（web-view 壳），无需 uni-app/上架 |
| 2026-06-02 | 补充患者端壳待办 | 新增 **§9.7** `his-mp-webview` 分阶段待办（A～F）、验收表与排错 |
| 2026-06-02 | 可演示验收P0/P1任务完成 | 修复start_demo.sh语法错误和服务名；添加患者登录账号(patient01/02)并关联patient表；Home.vue对接authStore；挂号页科室改调API；双端build验证通过 |
| 2026-06-02 | 完善his-mp-webview患者端壳 | 添加加载中/失败兜底页；添加环境区分注释；更新README完整使用步骤；演示说明已包含微信章节 |
| 2026-06-02 | 提交 8d9fb80 可演示验收收口 | 患者端 Home/挂号/科室 API、patient01 演示账号、双端 build、微信壳、演示说明与进度文档同步 |
| 2026-06-03 | 全项目状态核查并更新本文 | 对齐最新提交；修正前后矛盾表述；补充 SQL/迁移结构风险与最小服务集口径 |
| 2026-06-03 | 新增 **§十、项目待办总清单** | 汇总 §4/§6/§8/§9/§9.7/§4.3/Plan B 全部未完成项为 P0～P3 可勾选清单 |
| 2026-06-03 | 完成P0-08任务：补全start_demo.sh后端启动部分 | 添加Gateway、Registration、Schedule、Statistics四个Go服务启动命令；添加his-auth、his-user两个Java服务启动命令 |
| 2026-06-03 | 完成P0-04任务：修复演示数据脚本表结构一致性 | 将demo_data.sql和seed_data.sql中的表名sys_user/sys_role/sys_user_role修改为migrations定义的users/roles/user_roles |
| 2026-06-03 | 完成P0-05任务：确保患者账号正确加载到his_auth库 | 修改db_init.sh脚本，将用户相关数据插入到his_auth库，业务数据插入到对应数据库 |
| 2026-06-03 | 完成P0-09任务：start_demo.sh添加微信壳导入提示 | 在脚本末尾添加微信开发者工具导入提示，包含患者账号信息 |
| 2026-06-03 | 修复Java服务端口配置不一致 | 修复his-auth服务数据库端口从15432改为5432，与Docker Compose配置一致 |
| 2026-06-03 | 验证Go服务配置文件完整性 | 确认所有9个Go服务配置文件端口正确，与Gateway路由配置一致 |
| 2026-06-03 | 验证前端配置正确性 | 确认患者端(5174)和管理端(5175)的Vite代理配置正确，均指向Gateway(8080) |
| 2026-06-03 | 验证Go服务编译成功 | Gateway、Registration、Schedule、Statistics四个Go服务均编译成功 |
| 2026-06-03 | 修复his-user服务依赖问题 | 将his-user的ORM框架从MyBatis-Plus改为Spring Data JPA，与代码实现一致 |
| 2026-06-03 | 完成P0-06任务：科室数据初始化 | 创建demo_user_data.sql，将科室数据单独导入his_user库 |
| 2026-06-03 | 完成P0-07任务：排班数据初始化 | 更新db_init.sh脚本，确保排班数据正确导入his_schedule库 |
| 2026-06-03 | 完成P0-16/P0-17任务：补充服务启动命令 | 更新start_demo.sh，添加pharmacy等Go服务和Java服务的启动命令，支持FULL_MODE完整启动 |
| 2026-06-03 | 完成P0-01~P0-03任务：数据库初始化 | Docker基础设施启动，完整执行db_init.sh，17个数据库迁移完成，用户/角色/科室/排班/药品数据全部导入 |
| 2026-06-03 | 完成P0-10/P0-13~P0-15任务：Go服务启动 | Gateway(8080)、Registration(8083)、Schedule(8090)、Statistics(8095)四个Go服务启动成功 |
| 2026-06-03 | 修复数据库端口配置 | Go服务配置文件数据库端口从5432改为15432，与Docker映射一致 |
| 2026-06-03 | 完成P0-10/P0-13~P0-15任务：Go服务启动 | Gateway(8080)、Registration(8083)、Schedule(8090)、Statistics(8095)四个Go服务启动成功 |
| 2026-06-03 | 修复gRPC端口冲突 | Schedule服务gRPC端口从9090改为9091，避免与his-auth冲突 |
| 2026-06-03 | Java服务启动受网络限制 | Maven无法连接阿里云仓库下载依赖，his-auth和his-user服务暂无法启动 |
| 2026-06-03 | 配置Maven镜像源 | 已配置腾讯云、华为云和Maven官方镜像，但当前环境网络限制仍无法访问 |
| 2026-06-03 | Maven依赖下载成功 | 腾讯云镜像可用，his-auth和his-user服务构建成功 |
| 2026-06-03 | 修复数据库密码hash | 更新users表password_hash为正确的BCrypt hash，admin/patient01登录成功 |
| 2026-06-03 | 修复JWT密钥长度 | his-auth JWT secret从27字符增加到35字符，满足HS256要求 |
| 2026-06-03 | 修复his-user Nacos配置 | 禁用Nacos依赖，解决spring.config.import报错 |
| 2026-06-03 | 修复his-user gRPC端口冲突 | gRPC端口从9090改为9092，避免与his-auth冲突 |
| 2026-06-03 | Gateway添加HMAC-SHA JWT支持 | 修改security/jwt.go支持HMAC验证，与Java服务token互通 |
| 2026-06-03 | Statistics服务添加HTTP端点 | 添加/api/statistics/dashboard和/api/statistics/registration-trend HTTP端点 |
| 2026-06-03 | 完成端到端冒烟测试 | admin/patient01登录成功，Gateway代理Statistics成功，双端前端可访问 |
| 2026-06-03 | P0-11/P0-12完成 | his-auth(8081)和his-user(8082)Java服务启动成功 |
| 2026-06-03 | P0-18/P0-19完成 | 患者端(5174)和管理端(5175)前端服务启动成功 |
| 2026-06-03 | P0-21/P0-22完成 | admin和patient01登录验证通过 |
| 2026-06-03 | Registration服务添加HTTP端点 | 添加创建挂号和查询挂号HTTP端点 |
| 2026-06-03 | his-user添加SecurityConfig | 允许公开访问departments/patients端点 |
| 2026-06-03 | P0-23~P0-25完成 | 患者端首页登录态/在线挂号/我的挂号验证通过 |
| 2026-06-03 | 修复registrations表ID自增 | 添加序列支持挂号记录创建 |
| 2026-06-03 | Schedule服务添加HTTP端点 | 添加排班计划和号源查询HTTP端点 |
| 2026-06-03 | Pharmacy服务添加HTTP端点 | 添加药品列表和库存查询HTTP端点 |
| 2026-06-03 | 补充patients表数据 | 添加张三、李四患者档案数据 |
| 2026-06-03 | 修复schedule_plans表结构 | 添加deleted_at列支持软删除 |
| 2026-06-03 | P0-26/P0-28~P0-32完成 | 患者个人中心/管理端工作台/科室/排班/药品/患者列表验证通过 |
| 2026-06-03 | Gateway添加departments/tree路由 | 支持科室树查询 |
| 2026-06-03 | P0-33完成 | 管理端科室树验证通过 |
| 2026-06-03 | 全项目状态核查并同步本文 | 对齐 `4a97d6d`：P0 浏览器双端全部完成；更新 §一/§四/§六/§八/§九/§十 |
| 2026-06-03 | P1 演示体验完善（`3ebb595`） | Gateway 前端兼容路由；his-auth/his-user 健康检查；双端 build 复检；文档同步 |
| 2026-06-03 | 文档二次同步 | 对齐 `3ebb595`：更新 §十 P1 勾选、§1.6 健康检查、Windows 启动说明 |
| 2026-06-03 | 新增 **§10.9 企业级对齐待办** | ENT-P0～P2 共 28 项：CI/CD、Java 安全、JWT、Gateway 路径、测试、可观测、K8s 生产化等 |
| 2026-06-03 | 细化 **§10.3 / §10.4 / §10.9** 待办 | 子任务编号（a/b/c…）、DoD、工时、路由矩阵、服务清单 |
| 2026-06-03 | P1-01 完成：我的处方页 | 创建 prescriptionApi.ts，修改 MyPrescriptions.vue 使用正确 API |
| 2026-06-03 | P1-04 完成：挂号列表 API 参数统一 | 在 Registration 服务中添加 /api/registration/list 端点 |
| 2026-06-03 | P1-06 完成：Dashboard「最近挂号」 | 添加 visitDate 参数支持 |
| 2026-06-03 | P1-08 完成：演示 SQL 文档化 | 在 README.md 中添加数据库初始化说明 |
| 2026-06-03 | P1-09/P1-13 完成：端口与服务名对齐 | 修改 PostgreSQL 端口为 15432 |
| 2026-06-03 | P1-03 完成：扩展页（报告/随访/在线问诊） | 为 examination、followup、outpatient 服务添加 HTTP 端点 |
| 2026-06-03 | P1-11 完成：Statistics 趋势优化 | Gateway 添加 /statistics/trend 路由 |
| 2026-06-03 | P1-12 完成：其余服务 health | 更新 verify_demo.sh 增加可选服务检查 |
| 2026-06-03 | P3-TECH-1 完成：健康检查端点全服务统一 | 创建共享 HealthController，所有 Java 服务添加 @ComponentScan |
| 2026-06-03 | P3-TECH-7 完成：Java ORM 统一 | his-auth 从 MyBatis-Plus 改为 Spring Data JPA |

---

## 八、项目里程碑：MVP演示版本（基本功能可演示）

### 8.1 里程碑目标
以「**Web 管理端 + 患者端（H5，等同架构中的小程序患者渠道）可演示**」为验收目标，完成挂号、管理、处方等核心演示路径。

### 8.2 关键成果（Definition of Done）

| 项 | 代码/文档 | 联调验收 |
|----|-----------|----------|
| 后端核心服务（Auth/Registration/Schedule/Pharmacy 等） | ✅ | ✅ 最小集已本地验证（2026-06-03） |
| 前端患者端 API 与页面 | ✅ | ✅ P0-23～P0-26 |
| 前端管理端 API 与页面 | ✅ | ✅ P0-28～P0-33 |
| 微信患者端壳 `his-mp-webview` | ✅ | 🔶 见 **§10.4** P2-D |
| 端到端演示无阻塞错误（浏览器） | — | ✅ P0-27 |
| `docs/演示说明.md` + 演示 SQL | ✅ | ✅ 已拆分并按库灌数 |

- [x] **后端核心服务**：Auth、Registration、Schedule、Pharmacy 等已实现完整业务分层
- [x] **前端患者端**：10 页 API 对接；路由守卫就绪
- [x] **前端管理端**：8 页 API 对接；Dashboard/患者管理已接统计与用户服务
- [x] **系统集成（浏览器）**：双端演示流程已在本环境复现通过（2026-06-03）
- [x] **文档**：`docs/演示说明.md` 与本文 §9 已对齐；全项目待办见 **§十**

### 8.3 阶段分解与时间规划

| 阶段 | 主要任务 | 预计工时 | 依赖关系 | 交付物 | 状态 |
|------|----------|----------|----------|--------|------|
| **阶段1：后端核心服务实现** | 1. Auth服务完整实现（登录、注册、权限）<br>2. Registration服务完整实现（挂号、退号）<br>3. Schedule服务完整实现（排班查询）<br>4. Pharmacy服务完整实现（处方查询）<br>5. 所有服务main.go初始化逻辑补全<br>6. Proto代码生成与gRPC注册 | 2周 | 无 | 四个可运行的Go微服务 | ✅ 已完成 |
| **阶段2：前端患者端API对接** | 1. 登录页面对接Auth服务<br>2. 医生列表页对接Schedule服务<br>3. 挂号页面对接Registration服务<br>4. 处方页面对接Pharmacy服务<br>5. 错误处理与加载状态 | 1.5周 | 阶段1完成 | 可交互的患者端小程序 | ✅ 已完成 |
| **阶段3：前端管理端搭建** | 1. 项目初始化（Vue3 + TS + Ant Design Vue）<br>2. 登录页面对接Auth服务<br>3. 医生管理CRUD页面<br>4. 排班管理页面<br>5. 药品管理页面 | 1.5周 | 阶段1完成 | 可交互的管理端Web应用 | ✅ 已完成 |
| **阶段4：集成测试与演示准备** | 1. 端到端流程测试<br>2. 演示数据准备<br>3. 演示脚本编写<br>4. 问题修复与优化 | 1周 | 阶段2、3完成 | 可演示的系统与演示文档 | ✅ **浏览器双端已通过** |
| **阶段5：可演示验收收口** | 1. 修复启动脚本<br>2. 患者账号与数据<br>3. 双端冒烟<br>4. 文档与进度同步 | 3～5 人日 | 阶段4 | 可重复演示的管理端 + 患者 H5 | 🔶 **微信壳待验收** |

**总预计工时：6周（MVP 开发）+ 约 1 周（演示收口）**

### 8.4 成功标准
1. **功能完整性**：患者能够完成“登录 → 查看医生 → 选择医生挂号 → 查看处方”的完整流程
2. **管理功能**：管理员能够“登录 → 管理医生信息 → 设置排班 → 管理药品”
3. **系统稳定性**：演示过程中无崩溃、无数据丢失
4. **用户体验**：界面响应时间<2秒，操作流畅

### 8.5 风险与应对
| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 后端服务实现复杂度高 | 延期 | 优先实现核心链路，非核心功能后续迭代 |
| 前端API对接联调耗时 | 延期 | 后端先提供Mock数据，前端并行开发 |
| 跨服务数据一致性 | 功能异常 | 演示阶段使用本地事务，分布式事务后续实现 |
| 演示环境搭建问题 | 演示失败 | 提前准备Docker一键部署脚本 |

### 8.6 后续规划
可演示验收通过后：
1. **真机/正式小程序**（非本次需要）：上架、业务域名、uni-app 重写等；当前仅需开发者工具演示（患者端）时沿用 `his-mp-webview`
2. **完善测试与 CI**：集成测试、双端 E2E、前端 build 纳入流水线
3. **演示环境**：Docker Compose 一键拉起最小服务集 + 双前端静态资源
4. **性能与安全**：缓存、HTTPS、审计与监控（见第五～六阶段规划）

---

## 九、可演示验收清单（管理端 + 患者 H5）

### 9.1 演示环境（检查项）

> 与 **§10.2 P0** 对应；勾选请优先在 §十 统一维护。

- [x] Docker：`docker compose` 启动 `postgresql`、`redis`（`start_demo.sh` 已集成）
- [x] 数据库：`scripts/db/db_init.sh`（迁移 + `seed_data.sql` + 分库 demo SQL）
- [x] 后端最小集：Gateway `:8080`、his-auth `:8081`、his-user `:8082`、registration `:8083`、schedule `:8090`、statistics `:8095`
- [x] 前端：`npm install` 后分别 `npm run dev`（患者 5174、管理 5175）
- [ ] 可选验证：运行 `scripts/verify_demo.sh` 确认各端口健康（见 `docs/演示说明.md` §1.6）
- [ ] 药品管理页：需 `FULL_MODE=true ./scripts/start_demo.sh` 或单独启动 pharmacy（`:8087`）

### 9.2 管理端演示路径（必须通过）

| 步骤 | 页面 | 验收标准 |
|------|------|----------|
| 1 | 登录 | `admin` / `admin123` 登录成功进入工作台 |
| 2 | 工作台 | 今日挂号/就诊/处方/收入数字来自 statistics API，非全 0 占位 |
| 3 | 医生管理 | 列表可见 demo 医生；搜索、新增/编辑可提交 |
| 4 | 排班管理 | 可见未来排班；新增/批量排班可保存 |
| 5 | 药品管理 | 10 种 demo 药品；库存调整有反馈 |
| 6 | 患者管理 | 列表与详情可打开 |
| 7 | 科室/用户 | 科室树、用户列表可加载 |

### 9.3 患者端演示路径（必须通过）

| 步骤 | 页面 | 验收标准 |
|------|------|----------|
| 1 | 登录/注册 | 注册或预置患者账号登录成功 |
| 2 | 首页 | 登录后显示用户昵称（`Home.vue` 已绑 `authStore`） |
| 3 | 在线挂号 | 选科室 → 拉取当日号源 → 确认挂号返回序号 |
| 4 | 我的挂号 | 列表含刚创建记录 |
| 5 | 我的处方/费用 | 有数据时正常展示；无数据时友好空态 |

### 9.4 演示账号（目标态）

| 角色 | 用户名 | 密码 | 状态 |
|------|--------|------|------|
| 管理员 | admin | admin123 | ✅ 已在 seed/demo 中 |
| 医生 | doctor01～05 | admin123 | ✅ 已在 demo_data 中 |
| 患者 | patient01 | admin123 | ✅ 已在 demo_data 中（可直接登录） |

### 9.5 已知注意事项（非阻塞）

1. **`start_demo.sh` 默认模式**：启动 Gateway + Registration + Schedule + Statistics + his-auth + his-user + 双前端；**不含** pharmacy。药品页演示请 `FULL_MODE=true ./scripts/start_demo.sh` 或手动启动 pharmacy
2. **演示 SQL**：已对齐 migrations 表名（`users`/`roles`/`departments` 等）；`db_init.sh` 自动按库执行 `seed_data.sql`、`demo_user_data.sql`、`demo_registration_data.sql`、`demo_schedule_data.sql`
3. **患者账号**：`patient01`/`patient02` 由 `db_init.sh` 写入 `his_auth.users`；患者档案在 `his_user.patients`
4. **微信壳**：`frontend/his-mp-webview` 仅加载 H5；模拟器验收见 **§10.4**；Plan B uni-app 非当前必需

### 9.6 微信开发者工具演示（患者端）

1. `cd frontend/his-web-patient && npm run dev`（默认 `http://127.0.0.1:5174`）
2. 微信开发者工具 → **导入项目** → 选择 `frontend/his-mp-webview`（患者端 web-view 壳）
   - 具体参数与步骤见：`frontend/his-mp-webview/README.md`
3. **详情 → 本地设置** → 勾选 **不校验合法域名、web-view（业务域名）、TLS…**
4. 编译运行；壳内 `web-view` 即加载患者 H5（地址见 `pages/index/index.js`）

说明：不要求小程序审核发布；真机预览时需将 H5 地址改为本机局域网 IP，并同样依赖「不校验」或内网环境。壳项目分阶段待办见 **§9.7**；**全项目待办汇总**见 **§十**。

### 9.7 `frontend/his-mp-webview` 待办清单（患者端微信演示壳）

> **定位**：本子项目**只服务于患者端**（`his-web-patient`），不包含管理端（`his-web-admin`）。  
> **职责**：在微信开发者工具 / 真机预览中，通过 `<web-view>` 加载已运行的患者 H5；**不实现挂号、登录等业务逻辑**。  
> **业务实现位置**：`frontend/his-web-patient` + 后端 Gateway/微服务。  
> **子项目说明**：[`frontend/his-mp-webview/README.md`](../frontend/his-mp-webview/README.md)

#### 9.7.1 架构与依赖关系（先看懂再动手）

```
PC（服务端）
├── Gateway :8080          ← H5 内 /api 请求经 Vite 代理到此
├── 微服务（auth/registration/schedule/…）
└── his-web-patient :5174  ← Vite dev，患者业务页面

frontend/his-mp-webview（仅壳）
└── pages/index → <web-view src="http://…:5174" />
         │
         └── 内嵌加载 his-web-patient（同机 127.0.0.1 或局域网 IP）
```

| 依赖项 | 路径/端口 | 壳子项目是否负责 |
|--------|-----------|------------------|
| 患者 H5 | `frontend/his-web-patient`，`:5174` | ❌ 须先单独启动 |
| API 网关 | `:8080` | ❌ 须先启动后端最小集 |
| 演示数据 | `seed_data.sql`、`demo_data.sql` 等 | ❌ 与壳无关，但影响 H5 内登录/挂号 |
| web-view 地址 | `pages/index/index.js` → `DEFAULT_PATIENT_H5` | ✅ 本目录配置 |

#### 9.7.2 当前工程状态（基线）

| 文件/配置 | 状态 | 说明 |
|-----------|------|------|
| `app.json` / `app.js` / `app.wxss` | ✅ 已有 | 单页应用入口 |
| `pages/index/index.wxml` | ✅ 已有 | `<web-view>` + 加载中/失败兜底页 |
| `pages/index/index.js` | ✅ 已有 | 默认 `http://127.0.0.1:5174`；环境区分注释说明 |
| `pages/index/index.wxss` | ✅ 已有 | 加载/错误提示样式 |
| `project.config.json` | ✅ 已有 | `urlCheck: false`；`appid: touristappid`（游客模式） |
| `README.md` | ✅ 已更新 | 完整使用步骤、前置条件、文件说明、常见问题 |
| 加载中/失败兜底页 | ✅ 已完成 | web-view 白屏时显示加载提示和错误信息 |
| 环境区分（模拟器/真机 URL） | ✅ 已完成 | 代码注释说明模拟器/真机 URL 区分方式 |
| 与 `docs/演示说明.md` 联动 | ✅ 已完成 | 演示说明已包含微信开发者工具章节（§1.5） |
| 一键启动脚本纳入壳项目 | ✅ 已完成 | `start_demo.sh` 启后端+双前端并打印 mp-webview 导入提示；`verify_demo.sh` 可验健康 |

#### 9.7.3 待办总览（按执行顺序）

| 阶段 | 目标 | 预计 |
|------|------|------|
| **A** | 工具与环境就绪 | 0.5h |
| **B** | 患者 H5 + 后端在 PC 跑通（壳的前置条件） | 1～2h |
| **C** | 导入壳项目并在模拟器看到 H5 | 0.5h |
| **D** | 患者核心链路在 web-view 内验收 | 1h |
| **E** | 真机预览（可选） | 0.5～1h |
| **F** | 文档与工程收尾 | 0.5h |

---

#### 阶段 A：工具与环境（壳项目专用）

- [ ] **A1** 安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)（稳定版即可）
- [ ] **A2** 确认本仓库路径：`HIS-Mixed-Go-Java/frontend/his-mp-webview`（导入时选**该目录**，不要选仓库根目录）
- [ ] **A3** 了解 AppID 策略：
  - 当前 `project.config.json` 使用 `touristappid`（游客/测试，适合本地演示）
  - 若需「预览二维码」给多人扫码或稳定真机调试，需改为微信公众平台注册的测试号/正式 AppID，并在工具中填写
  - **硬性前提**：`touristappid` 主要用于本机开发工具演示；真机链路出现限制时，先排查 AppID/账号权限，再排查网络
- [ ] **A4** 打开工具后预先勾选（**详情 → 本地设置**）：
  - [ ] 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书
  - [ ] （建议）启用 ES6 转 ES5（与 `project.config.json` 中 `es6: true` 一致）

---

#### 阶段 B：前置——必须先跑通患者 H5 与 API（非本目录代码，但阻塞壳演示）

> 壳只负责「打开网页」；以下任一步失败，模拟器中会白屏或 H5 内接口报错。

- [ ] **B1** 启动基础设施：`docker compose` 拉起 `postgresql`、`redis`（见 `docker/docker-compose.yml`）
- [ ] **B2** 初始化数据库：`scripts/db/db_init.sh` + `backend/go/sql/seed_data.sql` + 按库执行 `demo_data.sql` 相关段落
- [ ] **B3** 启动**最小后端集**（至少）：Gateway `:8080`、`his-auth`、`his-user`、`registration`、`schedule`（挂号演示所需）
- [ ] **B4** 启动患者 H5：
  ```bash
  cd frontend/his-web-patient
  npm install
  npm run dev
  ```
- [ ] **B5** 浏览器自测：访问 `http://127.0.0.1:5174`，确认页面可打开；尝试登录/注册（无 502/404）
- [ ] **B6** 确认 Vite 代理：患者 H5 请求 `/api/*` 能到 Gateway（`vite.config.ts` 中 `proxy '/api' → localhost:8080`）
- [x] **B7** 患者端 P1 已完成：`Home.vue` 登录态、`Registration.vue` 科室 API、`patient01` 账号（见 §4.2、§9.4）

##### B2 补充：最小演示 SQL 执行矩阵（建议）

> 目的：避免“脚本执行了但链路仍缺数据”。先保证登录 + 挂号核心链路所需数据。

| 库 | 脚本/动作 | 必须性 | 作用 |
|----|-----------|--------|------|
| `his_auth` | 执行 `seed_data.sql`；**患者/医生登录账号**（demo 中 `sys_user` 段，需确认表存在） | ✅ 必须 | admin / patient01 登录 |
| `his_user` | 科室数据（`departments`）；患者档案与 `/departments` API | ✅ 必须 | 挂号选科室、个人中心 |
| `his_registration` | `demo_data.sql` 中挂号/排队等业务表段落（与 migrations 对齐后执行） | 🔶 按需 | 我的挂号列表 |
| `his_schedule` | 排班/号源数据（demo 或管理端录入） | ✅ 必须 | 在线挂号选医生 |
| `his_pharmacy`/`his_billing` | 若要验收“我的处方/费用”，执行对应演示数据 | 🔶 按需 | 处方与费用页非空 |

> 最小闭环（D3~D5）至少确保：`his_auth` + `his_registration` 数据齐全。

---

#### 阶段 C：配置并导入 `his-mp-webview`

- [ ] **C1** 核对 H5 地址常量：编辑 [`pages/index/index.js`](../frontend/his-mp-webview/pages/index/index.js) 中 `DEFAULT_PATIENT_H5`
  - **仅开发者工具模拟器（H5 与工具同机）**：`http://127.0.0.1:5174`
  - **真机预览（手机访问 PC）**：改为 `http://<PC局域网IP>:5174`（如 `http://192.168.1.100:5174`），且患者 H5 须 `vite --host` 监听 `0.0.0.0`（见阶段 E）
- [ ] **C2** 微信开发者工具 → **导入** → 目录选择 `frontend/his-mp-webview`
- [ ] **C3** 编译模式选「普通编译」，确认无红色报错（`app.json` 页面路径、`pages/index` 四件套齐全）
- [ ] **C4** 模拟器启动后应出现患者 H5 首页（与浏览器打开 5174 一致）；若白屏见 **§9.7.6 排错**
- [ ] **C5** （可选）修改 `project.config.json` 中 `projectname` / 描述，与课程命名一致

---

#### 阶段 D：在 web-view 内验收患者演示路径（Definition of Done）

在**微信开发者工具模拟器**中完成（不要求上架）：

| 编号 | 验收项 | 操作 | 通过标准 |
|------|--------|------|----------|
| D1 | 壳加载 H5 | 打开小程序项目即进入 web-view | 可见患者端 UI，非空白、非「无法打开」 |
| D2 | 未登录态 | 首页/需登录页 | 可跳转登录页或提示登录（与 H5 行为一致） |
| D3 | 登录 | 使用演示患者账号或注册 | 登录成功，token 写入（H5 内 `localStorage`） |
| D4 | 在线挂号 | 选科室 → 医生/号源 → 提交 | 返回挂号序号或成功提示 |
| D5 | 我的挂号 | 进入「我的挂号」 | 列表含刚创建记录或合理空态 |
| D6 | 个人中心 | 打开 Profile | 显示用户信息；退出后回到未登录态 |
| D7 | API 无跨域死链 | 先用桌面浏览器访问 H5 并看 Network，再在开发者工具复测关键动作 | 无持续 401/502；`/api` 指向 PC:8080 |

- [ ] **D1** ～ **D7** 全部勾选后，视为 **`his-mp-webview` 演示验收通过**

---

#### 阶段 E：真机预览（可选，未正式发布小程序）

- [ ] **E1** PC 与手机连接**同一 WiFi**
- [ ] **E0** 确认真机前提：AppID 与体验成员权限可用（`touristappid` 下若受限，先改为测试号/正式 AppID）
- [ ] **E2** 查 PC 局域网 IP（如 `ipconfig` → IPv4）
- [ ] **E3** 修改 `DEFAULT_PATIENT_H5` 为 `http://<PC_IP>:5174`
- [ ] **E4** 患者 H5 以 host 模式启动（在 `his-web-patient` 的 `vite.config.ts` 增加 `server.host: true`，或 `npm run dev -- --host`）
- [ ] **E5** 手机浏览器先访问 `http://<PC_IP>:5174`，确认能打开（排除防火墙拦截）
- [ ] **E6** 微信开发者工具 → **预览** → 手机扫码；真机调试设置中允许不校验域名（与模拟器相同）
- [ ] **E7** 真机重复阶段 D 中 D3～D6 至少各 1 次

> 说明：未配置 HTTPS 业务域名时，**仅适合开发/答辩演示**；正式环境需域名 + 备案 + 小程序后台配置，不在本子项目范围。

---

#### 阶段 F：工程与文档收尾（建议做，便于他人复现）

- [x] **F1** 更新 [`frontend/his-mp-webview/README.md`](../frontend/his-mp-webview/README.md)：补充「模拟器 vs 真机」两套 URL、防火墙提示、与 §9.7 交叉引用
- [x] **F2** 在 [`docs/演示说明.md`](演示说明.md) 增加小节「患者端 · 微信开发者工具（his-mp-webview）」，步骤与 §9.6、§9.7 一致
- [x] **F3** （可选）`pages/index` 增加加载提示：web-view 加载前显示「正在连接患者服务…」；H5 不可达时提示检查 5174 与 `DEFAULT_PATIENT_H5`
- [ ] **F4** （可选）将 `project.config.private.json`（若生成）加入 `.gitignore`，避免本机路径泄露
- [ ] **F5** （可选）在 `scripts/start_demo.sh` 注释块中增加：启动 `his-web-patient` + 打印「请导入 frontend/his-mp-webview」
- [x] **F6** 在本文件 §7 进度记录中追加验收相关记录（见 2026-06-02/06-03 条目）；模拟器/真机勾选日期待实测后填写

---

#### 9.7.4 本子项目文件级检查表（改代码时对照）

| 待办 | 文件 | 动作 |
|------|------|------|
| [ ] | `pages/index/index.js` | 确认/修改 `DEFAULT_PATIENT_H5`；真机改局域网 IP |
| [ ] | `pages/index/index.wxml` | 确认 `web-view` 绑定 `patientUrl` |
| [ ] | `pages/index/index.json` | 导航栏标题「HIS 患者端」是否满足演示口径 |
| [ ] | `app.json` | `pages` 仅 `pages/index/index`；`window` 标题与配色 |
| [ ] | `project.config.json` | `urlCheck: false`；按需更换 `appid` |
| [ ] | `README.md` | 与 §9.7 步骤同步 |

**明确不在此目录实现的功能（避免误做）：**

- 管理端页面、管理员登录
- 直接 `wx.request` 调 HIS API（应走 H5 内已有封装）
- uni-app 业务页、分包、tabBar 多页业务（多页请走 Plan B `his-uni-patient`）

---

#### 9.7.5 与相邻子项目分工（避免混淆）

| 子项目 | 角色 | 你是否应在该目录改业务 |
|--------|------|------------------------|
| `his-web-patient` | 患者端 H5 业务与 API | ✅ 是 |
| `his-web-admin` | 管理端 Web | ❌ 与 mp-webview 无关 |
| `his-mp-webview` | 患者端微信壳（web-view） | ⚠️ 仅改 URL、壳 UI、工具配置 |
| `his-uni-patient`（Plan B，未建） | uni-app 原生患者小程序 | ❌ 非 web-view 方案 |

---

#### 9.7.6 常见问题排错（壳 + H5）

| 现象 | 可能原因 | 处理 |
|------|----------|------|
| 模拟器白屏 | H5 未启动或端口不是 5174 | 执行阶段 B4、B5 |
| 提示无法打开 web-view | 未勾选「不校验合法域名…」 | 阶段 A4 |
| H5 能开但登录/挂号失败 | Gateway/微服务未起或 demo 数据未导入 | 阶段 B2、B3 |
| 真机白屏、模拟器正常 | 仍用 `127.0.0.1` 或防火墙拦截 | 阶段 E3～E5 |
| 真机扫码后无法进入/受限 | AppID 或体验成员权限不足（`touristappid` 限制） | 阶段 E0，改测试号/正式 AppID 后重试 |
| H5 内接口 404 | Vite 未代理或 Gateway 路由缺失 | 检查 `his-web-patient/vite.config.ts` 与 Gateway |
| 管理端误导入此目录 | 目录选错 | 管理端用 `his-web-admin`，勿导入 `his-mp-webview` |

---

#### 9.7.7 完成度勾选（汇总）

| 类别 | 完成标准 |
|------|----------|
| 骨架 | 阶段 C 全部完成 |
| 模拟器演示 | 阶段 D 全部完成 |
| 真机（可选） | 阶段 E 全部完成 |
| 文档 | 阶段 F 中 F1、F2 完成 |

**当前建议状态**：壳工程 ✅｜加载/错误兜底 ✅｜文档联动 ✅｜浏览器双端 ✅｜模拟器验收 🔶（§10.4 P2-D）｜真机 🔶（可选）

---

## 十、项目待办总清单（汇总）

> 每条待办尽量包含：**目标 → 子任务（可勾选）→ 验收标准（DoD）→ 工时/依赖**。  
> 演示验收：**P0 → P1 → P2**；生产/企业级：**§10.9 ENT-***。

### 10.1 统计概览（截至 2026-06-03）

| 优先级 | 含义 | 主项 | 未完成子项（约） |
|--------|------|------|------------------|
| **P0** | 浏览器可演示验收 | 33 | ✅ 0 |
| **P1** | 演示体验完善 | 13 | 18 |
| **P2** | 微信壳/文档收尾 | 19 | 19 |
| **P3** | 第二阶段架构 | 12+ | 见 §10.5（与 ENT 映射） |
| **ENT-P0** | 企业级阻塞 | 8 | 42 |
| **ENT-P1** | 企业级 staging | 12 | 48 |
| **ENT-P2** | 企业级生产 polish | 8 | 32 |
| **条件** | Plan B uni-app | 7 | 7 |

---

### 10.2 P0 — 可演示验收（当前必须完成）

#### 10.2.1 环境与一键启动

- [x] **P0-01** 本机启动 Docker，拉起 `postgresql`、`redis`（`docker/docker-compose.yml`）
- [x] **P0-02** 执行 `backend/go/scripts/db_init.sh`（或 `scripts/db/db_init.sh`，以仓库实际路径为准）
- [x] **P0-03** `his_auth` 执行 `backend/go/sql/seed_data.sql`
- [x] **P0-04** 核对 `demo_data.sql` 与 `migrations/*.sql` 表结构一致后再灌库（见 §9.5 第 2 条）
- [x] **P0-05** 将 `demo_data.sql` 中 **登录账号段**（`sys_user` / `patient01` / `doctor01` 等）拆出或同步灌入 `his_auth`（避免 patient01 无法登录）
- [x] **P0-06** `his_user` 灌入科室等数据，保证 `/api/departments` 有返回
- [x] **P0-07** `his_schedule` 灌入或管理端录入排班/号源，保证挂号可选医生
- [x] **P0-08** 补全 `scripts/start_demo.sh` 第 4 步：取消注释并启动 Gateway + 最小 Java/Go 微服务（或文档化固定用手动命令）
- [x] **P0-09** `start_demo.sh` 结束时打印：患者端 5174、管理端 5175、**请导入 `frontend/his-mp-webview`**

#### 10.2.2 后端最小服务集（运行时）

- [x] **P0-10** 启动 Gateway（`:8080`）
- [x] **P0-11** 启动 `his-auth`（`:8081`）
- [x] **P0-12** 启动 `his-user`（`:8082`）
- [x] **P0-13** 启动 `registration`（`:8083`）
- [x] **P0-14** 启动 `schedule`（`:8090`）
- [x] **P0-15** 启动 `statistics`（`:8095`）
- [x] **P0-16** 管理端药品页：启动 `pharmacy`（`:8087`）（冒烟时已验证；默认脚本需 FULL_MODE）
- [x] **P0-17** 管理端患者列表：`his-user` 患者端点已可用

#### 10.2.3 前端启动与构建

- [x] **P0-18** `frontend/his-web-patient`：`npm install && npm run dev`（5174）
- [x] **P0-19** `frontend/his-web-admin`：`npm install && npm run dev`（5175）
- [x] **P0-20** 浏览器验证患者 H5：`http://127.0.0.1:5174`，`/api` 代理到 Gateway 无持续 502/404
- [x] **P0-21** 本机复现：管理端 `admin` / `admin123` 登录进入工作台
- [x] **P0-22** 本机复现：患者 `patient01` / `admin123` 登录成功

#### 10.2.4 患者端核心链路冒烟（浏览器）

- [x] **P0-23** 患者端：首页登录后显示昵称
- [x] **P0-24** 患者端：在线挂号（选科室 → 医生/号源 → 提交 → 返回序号）
- [x] **P0-25** 患者端：我的挂号列表含刚创建记录
- [x] **P0-26** 患者端：个人中心可查看信息并退出登录
- [x] **P0-27** 标记 **§8.2「系统集成」** 在本环境通过（双端 + 数据 + 后端，2026-06-03）

#### 10.2.5 管理端核心链路冒烟（浏览器）

- [x] **P0-28** 管理端：工作台统计数据非全 0（statistics API）
- [x] **P0-29** 管理端：医生列表可见 demo 医生，搜索/编辑可提交
- [x] **P0-30** 管理端：排班列表可见，新增/批量排班可保存
- [x] **P0-31** 管理端：药品列表 10 条 demo，库存操作有反馈
- [x] **P0-32** 管理端：患者列表与详情可打开
- [x] **P0-33** 管理端：科室树、用户列表可加载

---

### 10.3 P1 — 演示体验完善（建议完成）

#### 10.3.0 已完成

- [x] **P1-05** 生产构建复检：`npm run build`（2026-06-03 双端通过）
- [x] **P1-07** Windows/PowerShell 启动说明：`docs/演示说明.md` §7.1
- [x] **P1-10** 浏览器端到端验收：2026-06-03（P0-27）
- [x] **P1-12** 核心 health（部分）：Gateway `/api/health`；Go `/health`；his-auth `/api/auth/health`；his-user `/api/patients/health`

#### P1-01 我的处方页（患者端）

| 属性 | 说明 |
|------|------|
| **目标** | `patient01` 登录后「我的处方」有数据或友好空态，无 404/502 |
| **依赖** | `FULL_MODE=true ./scripts/start_demo.sh` 或手动启 pharmacy；Gateway 路由 `/api/pharmacy/prescriptions` |
| **工时** | 0.5 人日 |

- [ ] **P1-01a** 启动 `pharmacy`（`:8087`），确认 `GET http://localhost:8087/health` 正常
- [ ] **P1-01b** 经 Gateway 调 `GET /api/pharmacy/prescriptions?patientId=<id>` 返回 200（非 502）
- [ ] **P1-01c** 若无业务数据：在 `his_pharmacy` 补 demo 处方或与 `demo_data.sql` 对齐
- [ ] **P1-01d** 打开 `frontend/his-web-patient` → 我的处方，列表/空态 UI 正常
- [ ] **P1-01e** **DoD**：Network 无红字；有数据时展示药品/用法；无数据时提示「暂无处方」

#### P1-02 费用查询页（患者端）

| 属性 | 说明 |
|------|------|
| **目标** | 「费用查询」可拉账单/支付记录或空态 |
| **依赖** | 启动 `his-billing`（`:8086`）；Gateway `/api/billing/bill-items`、`/payments` |
| **工时** | 0.5 人日 |

- [ ] **P1-02a** `FULL_MODE` 或手动 `mvn spring-boot:run -pl his-billing`
- [ ] **P1-02b** Gateway 代理 `GET /api/billing/bill-items?patientId=` 通
- [ ] **P1-02c** 补 `his_billing` 演示数据（可选）
- [ ] **P1-02d** 患者端 `MyBills.vue` 联调通过
- [ ] **P1-02e** **DoD**：未支付金额/列表其一可用；错误有 Toast

#### P1-03 扩展页（报告 / 随访 / 在线问诊）

| 页面 | 后端服务 | 端口 | Gateway 前缀 | 前端 API |
|------|----------|------|--------------|----------|
| 检查报告 | examination | 8088 | `/api/examination/reports` | `report.ts` |
| 随访 | followup | 8092 | `/api/followup/plans` | `followup.ts` |
| 在线问诊 | outpatient | 8091 | `/api/outpatient/consultations` | `consultation.ts` |

- [ ] **P1-03a** 按需启动上述 Go 服务（`FULL_MODE` 或单独 `go run`）
- [ ] **P1-03b** 各页至少验证：**有数据展示** 或 **空态 + 无 500**
- [ ] **P1-03c** **DoD**：三页均可在演示中「点开不报错」

#### P1-04 挂号列表 API 参数统一

| 属性 | 说明 |
|------|------|
| **目标** | `MyAppointments.vue` 与 `registrationApi.getRegistrations` 参数一致 |
| **涉及** | `frontend/his-web-patient/src/views/appointment/MyAppointments.vue`、`src/api/registration.ts` |
| **工时** | 0.25 人日 |

- [ ] **P1-04a** 确认调用方式为 `getRegistrations(patientId, page, pageSize)`（非对象/位置混用）
- [ ] **P1-04b** 与 Registration HTTP 端点 query 参数对齐（`patientId`、`page`、`pageSize`）
- [ ] **P1-04c** **DoD**：我的挂号与管理端 Dashboard 拉同一 patient 数据一致

#### P1-06 Dashboard「最近挂号」

- [ ] **P1-06a** 核对 `frontend/his-web-admin/src/api/statistics.ts` 中 `getRecentRegistrations` 路径
- [ ] **P1-06b** 对接 `GET /api/registration/list`（或 statistics 聚合接口），分页参数正确
- [ ] **P1-06c** `Dashboard.vue` 展示最近 N 条，无 mock 硬编码
- [ ] **P1-06d** **DoD**：新建挂号后 Dashboard 刷新可见

#### P1-08 / P1-09 / P1-11 / P1-13 文档与配置

**P1-08 演示 SQL 文档化**

- [ ] **P1-08a** 在根 `README.md` 增加「数据库初始化」小节：指向 `scripts/db/db_init.sh`
- [ ] **P1-08b** 列出分库脚本：`seed_data.sql`、`demo_user_data.sql`、`demo_registration_data.sql`、`demo_schedule_data.sql`
- [ ] **P1-08c** 说明默认连接 `localhost:15432` / 用户 `his_admin`

**P1-09 / P1-13 端口与服务名对齐**

- [ ] **P1-09a** 对照 `docker/docker-compose.yml` 与 `backend/go/configs/gateway.yaml` 整理端口表
- [ ] **P1-09b** 同步 `README.md`、`docs/演示说明.md` §1.3、本文 §9.1
- [ ] **P1-09c** **DoD**：三处文档端口零矛盾

**P1-11 Statistics 趋势优化**

- [ ] **P1-11a** 确认前端 `statistics.ts` 请求粒度（日/周/月）与后端 `trendDateExpr` 一致
- [ ] **P1-11b** 大数据量下 SQL 索引/分页（可选 EXPLAIN）
- [ ] **P1-11c** **DoD**：管理端趋势图切换粒度数据形状正确

**P1-12 续：其余服务 health（与 ENT-P0-06 重叠，演示侧重 verify 脚本）**

- [ ] **P1-12a** 更新 `scripts/verify_demo.sh`：增加 pharmacy/billing 可选检查项
- [ ] **P1-12b** 文档 §1.6 与脚本输出一致

---

### 10.4 P2 — 微信壳 `frontend/his-mp-webview`（患者端演示）

> 明细步骤亦见 **§9.7**。以下为带 **DoD** 的勾选清单。

#### 10.4.0 前置条件（壳验收前 5 分钟自检）

- [ ] **P2-PRE-1** `./scripts/start_demo.sh` 或等价环境已启：5174 / 8080 / auth+user+registration
- [ ] **P2-PRE-2** 浏览器可打开 `http://127.0.0.1:5174` 且 `patient01` 能登录
- [ ] **P2-PRE-3** 微信开发者工具已安装，可导入 `frontend/his-mp-webview`

#### 10.4.1 阶段 A：工具与环境

| 编号 | 子任务 | DoD |
|------|--------|-----|
| P2-A1 | [ ] 安装[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) | 可创建/导入小程序项目 |
| P2-A2 | [ ] 导入目录 `frontend/his-mp-webview`（非仓库根） | 项目树可见 `pages/index` |
| P2-A3 | [ ] 确认 AppID：`touristappid` 或测试号 | 编译无 AppID 致命错误 |
| P2-A4 | [ ] 勾选「不校验合法域名、web-view、TLS…」 | 见 §9.6 |
| P2-A4b | [ ] （建议）ES6 转 ES5 | 与 `project.config.json` 一致 |

#### 10.4.2 阶段 B：前置

- [x] **P2-B1** §10.2 P0 浏览器自测已完成；壳验收前再确认 5174/8080 仍在线

#### 10.4.3 阶段 C：导入壳

| 编号 | 子任务 | DoD |
|------|--------|-----|
| P2-C1 | [ ] `pages/index/index.js` 中 `DEFAULT_PATIENT_H5=http://127.0.0.1:5174` | 与 Vite 端口一致 |
| P2-C2 | [ ] 导入并「普通编译」 | 无红色报错 |
| P2-C3 | [ ] 四件套齐全：`index.js/wxml/wxss/json` | 编译通过 |
| P2-C4 | [ ] 模拟器出现患者 H5 首页 | 非白屏、非「无法打开」 |
| P2-C5 | [ ] （可选）改 `projectname` | 与课程命名一致 |

#### 10.4.4 阶段 D：web-view 内验收

| 编号 | 操作 | DoD |
|------|------|-----|
| P2-D1 | [ ] 打开项目即见 web-view | 可见患者 UI |
| P2-D2 | [ ] 未登录访问需登录页 | 与浏览器 H5 行为一致 |
| P2-D3 | [ ] `patient01` / `admin123` 登录 | token 写入 localStorage |
| P2-D4 | [ ] 在线挂号全流程 | 返回序号或成功提示 |
| P2-D5 | [ ] 我的挂号 | 含新记录或合理空态 |
| P2-D6 | [ ] 个人中心 + 退出 | 信息可见；退出回未登录 |
| P2-D7 | [ ] Network 无持续 401/502 | 关键 API 经 Gateway:8080 |
| P2-D | [ ] **汇总** D1～D7 全勾选 | **微信壳模拟器验收通过** |

#### 10.4.5 阶段 E：真机（可选）

| 编号 | 子任务 | DoD |
|------|--------|-----|
| P2-E0 | [ ] AppID/体验成员可用 | 非 tourist 限制时可扫码 |
| P2-E1 | [ ] PC 与手机同 WiFi | — |
| P2-E2 | [ ] `ipconfig` 得 PC IPv4 | — |
| P2-E3 | [ ] `DEFAULT_PATIENT_H5` 改为 `http://<IP>:5174` | — |
| P2-E4 | [ ] H5：`npm run dev -- --host` | 手机浏览器可开 5174 |
| P2-E5 | [ ] 手机浏览器先测 H5 | 排除防火墙 |
| P2-E6 | [ ] 微信预览扫码 | 真机进入壳 |
| P2-E7 | [ ] 真机重复 D3～D6 | 各至少 1 次 |

#### 10.4.6 阶段 F：工程收尾

- [ ] **P2-F4** `project.config.private.json` → `.gitignore`
- [x] **P2-F5** `start_demo.sh` 打印 mp-webview 提示
- [ ] **P2-F6** §7 填写模拟器/真机验收日期与操作人

#### 10.4.7 文件级确认

| 文件 | 检查点 | [ ] |
|------|--------|-----|
| `pages/index/index.js` | `DEFAULT_PATIENT_H5`、真机改 IP | |
| `pages/index/index.wxml` | `web-view` 绑定 `patientUrl` | |
| `pages/index/index.json` | 标题「HIS 患者端」 | |
| `app.json` | 仅 `pages/index/index` | |
| `project.config.json` | `urlCheck: false` | |
| `README.md` | 与 §9.7 一致 | |

---

### 10.5 P3 — 第二阶段及架构能力（非演示阻塞）

> 与 **§10.9 ENT-*** 重叠项见 **§10.9.5 映射表**；实施细节以 §10.9 子任务为准。

#### 10.5.1 测试

- [ ] **P3-T1** Java 单元测试（9 个服务）
- [ ] **P3-T2** 业务链路集成测试（超越当前 4 个 Gateway 用例）
- [ ] **P3-T3** 前端 E2E（管理端 + 患者端关键路径）
- [ ] **P3-T4** 将双端 `npm run build` 纳入 CI

#### 10.5.2 中间件与微服务增强

- [ ] **P3-M1** Nacos Go SDK：服务注册与发现
- [ ] **P3-M2** Nacos 动态配置加载
- [ ] **P3-M3** RabbitMQ Consumer 与幂等
- [ ] **P3-M4** Seata 分布式事务（Java AT）
- [ ] **P3-M5** Java 服务 gRPC Service 实现类（当前以 REST 为主）

#### 10.5.3 部署

- [ ] **P3-D1** K8s 镜像实际构建与部署验证
- [ ] **P3-D2** Docker Compose 一键拉起最小服务集 + 双前端静态资源
- [ ] **P3-D3** 修复 Docker 多服务/端口历史问题（§5.1 遗留项复核）

#### 10.5.4 技术债务（§5.3 / §5.4）

- [ ] **P3-TECH-1** 健康检查端点全服务统一（his-auth/his-user/Go 已部分实现，见 P1-12）
- [ ] **P3-TECH-2** 配置热更新
- [ ] **P3-TECH-3** 日志级别动态调整
- [ ] **P3-TECH-4** OpenTelemetry 链路追踪
- [ ] **P3-TECH-5** Gateway 熔断器（Circuit Breaker）
- [ ] **P3-TECH-6** Gateway 负载均衡（多实例）
- [ ] **P3-TECH-7** Java ORM 统一（MyBatis-Plus vs JPA）
- [ ] **P3-TECH-8** Statistics 服务趋势查询深度优化

#### 10.5.5 Go 服务表内「剩余」项（代码已具备，非演示阻塞）

- [ ] **P3-G1** Gateway 动态路由配置（当前 viper 已加载，可继续完善）
- [ ] **P3-G2** Proto RPC 覆盖面扩展（当前 proto 方法较少）

---

### 10.6 条件触发 — Plan B：`his-uni-patient`（uni-app 原生小程序）

> **默认不启用**。仅当评审明确要求 uni-app 且不接受 web-view 壳时，按本节执行。详见 **Plan B** 全文。

- [ ] **PB-TRIGGER** 确认满足 Plan B 启动条件（§Plan B B.7）
- [ ] **PB-A** 新建 `frontend/his-uni-patient`，初始化 manifest / pages.json / request.ts
- [ ] **PB-B** 登录页 + 路由守卫 + token 存储
- [ ] **PB-C** 在线挂号页（科室 API + 号源 + 提交挂号）
- [ ] **PB-D** 我的挂号 + 个人中心
- [ ] **PB-E** 真机调试 + 更新 `docs/演示说明.md`
- [ ] **PB-DONE** Plan B Definition of Done 验收通过

---

### 10.7 验收里程碑勾选（与 §8 / §9 对应）

| 里程碑 | 对应待办 | 状态 |
|--------|----------|------|
| MVP 代码完成 | — | ✅ |
| 演示说明与进度文档对齐 | P1-09 | 🔶 |
| 管理端浏览器演示通过 | §10.2.5 P0-28～P0-33 | ✅ 2026-06-03 |
| 患者端浏览器演示通过 | §10.2.4 P0-23～P0-26 | ✅ 2026-06-03 |
| 微信壳模拟器演示通过 | §10.4.4 P2-D1～D7 | [ ] **当前** |
| 微信壳真机演示（可选） | §10.4.5 P2-E | [ ] |
| 端到端「系统集成」签字（浏览器） | P0-27、§8.2 | ✅ 2026-06-03 |
| 可演示验收阶段关闭 | P0 ✅ + P2-D | 🔶 **差微信壳** |
| 企业级生产就绪（首批 ENT-P0） | §10.9 ENT-P0 | [ ] **未开始** |

---

### 10.8 已完成待办（归档，无需重复做）

以下项已在代码/文档中完成，仅作归档：

- [x] Dockerfile 多服务构建修复
- [x] Proto 代码生成（Go + Java）
- [x] 核心 Go/Java 业务骨架与主要 API
- [x] 患者端 10 页 + 管理端 8 页 API 对接
- [x] `Home.vue` authStore、`Registration.vue` 科室 API、`department.ts`
- [x] `patient01` / `patient02` 写入 `demo_data.sql`
- [x] `his-mp-webview` 壳：加载中/失败页、环境注释、README、演示说明 §1.5
- [x] `start_demo.sh`：一键启 infra+DB+最小后端+双前端；`FULL_MODE` 扩展服务
- [x] `verify_demo.sh` 健康检查脚本
- [x] 演示 SQL 拆分与 migrations 表名对齐（2026-06-03）
- [x] Gateway HTTP 路由 + JWT HMAC 互通
- [x] P0 浏览器双端冒烟（P0-23～P0-33，2026-06-03）
- [x] P1 Gateway 前端兼容路由 + 部分健康检查（`3ebb595`）
- [x] P1 双端 `npm run build` 复检（2026-06-03）
- [x] 已知缺陷表中 6 项高/中优先级修复（§5.4）

---

### 10.9 企业级对齐待办（生产就绪）

> **定位**：在 MVP 可演示基础上，向企业级/生产环境对齐。与 §10.2 演示 P0 **编号独立**（本段前缀 **ENT-**）。  
> **核查依据**：2026-06-03 代码审查（`3ebb595`）；已具备基础：Proto/错误码/Go 单测约 79 例、Gateway 限流/优雅关闭、K8s 骨架、12-factor 部分 env。  
> **建议顺序**：ENT-P0（安全+路由+CI）→ ENT-P1（测试+可观测+API 规范）→ ENT-P2（弹性+生产 K8s）。

**主项索引**

| 主项 | 子任务前缀 | 小节 |
|------|------------|------|
| CI/CD | ENT-P0-01a～h | §10.9.2 |
| Java Security ×7 | ENT-P0-02a～d | §10.9.2 |
| his-user 鉴权 | ENT-P0-03a～d | §10.9.2 |
| JWT 统一 | ENT-P0-04a～f | §10.9.2 |
| Gateway 路径 | ENT-P0-05a～e | §10.9.2 |
| health/ready | ENT-P0-06a～f | §10.9.2 |
| Java 测试 | ENT-P0-07a～e | §10.9.2 |
| K8s Secret | ENT-P0-08a～d | §10.9.2 |
| ENT-P1-01～12 | 各 a～f | §10.9.3 |
| ENT-P2-01～08 | 各 a～f | §10.9.4 |

#### 10.9.1 已有基础（无需重复做）

- [x] 架构与接口文档（`docs/架构设计文档.md`、`docs/API接口规范文档.md`、`docs/数据库设计文档.md`）
- [x] Go 统一响应封装（`backend/go/pkg/response/response.go`）、错误码体系（`pkg/errors/errors.go`）
- [x] Gateway：JWT 白名单、CORS、RequestID、Recovery、全局限流、优雅关闭（`cmd/gateway/main.go`）
- [x] Go 业务层单测约 79 例；集成测试脚手架（`testing/api/auth_flow_test.go`，需 `HIS_INTEGRATION_TEST=true`）
- [x] Docker 多阶段构建 + Compose + K8s base 清单（探针/资源 limit 骨架）
- [x] 本地质量脚本（`scripts/check.sh`、`Makefile` build/test）

---

#### 10.9.2 ENT-P0 — 生产/安全阻塞（优先）

> **建议实施顺序**：ENT-P0-04 → ENT-P0-05 → ENT-P0-02/03 → ENT-P0-06/08 → ENT-P0-01/07

---

##### ENT-P0-01 建立 CI/CD 流水线

| 属性 | 说明 |
|------|------|
| **工时** | 1～2 人日 |
| **依赖** | ENT-P0-07（至少 smoke test 再门禁） |

- [ ] **ENT-P0-01a** 新增 `.github/workflows/ci.yml`（或 Jenkinsfile）
- [ ] **ENT-P0-01b** Job：`go test ./...`（`backend/go`）
- [ ] **ENT-P0-01c** Job：`mvn -B test`（`backend/java`，可先 `-pl his-auth,his-user` 试点）
- [ ] **ENT-P0-01d** Job：`npm ci && npm run build`（双前端）
- [ ] **ENT-P0-01e** Job：`scripts/check.sh`（gofmt/vet/build）
- [ ] **ENT-P0-01f** Job：Docker build `docker/Dockerfile.go` + `Dockerfile.java`（`SERVICE_NAME` 参数）
- [ ] **ENT-P0-01g** （可选）`kubeval` / `kubectl apply --dry-run` 校验 `docker/k8s/base/`
- [ ] **ENT-P0-01h** **DoD**：PR 必过 CI；main 分支 badge 可展示

---

##### ENT-P0-02 七个 Java 服务补 SecurityConfig + JWT

| 服务 | 模块路径 | 当前状态 |
|------|----------|----------|
| his-clinic | `backend/java/his-clinic` | 无 SecurityConfig |
| his-emr | `backend/java/his-emr` | 无 |
| his-prescription | `backend/java/his-prescription` | 无 |
| his-billing | `backend/java/his-billing` | 无 |
| his-inpatient | `backend/java/his-inpatient` | 无 |
| his-health-record | `backend/java/his-health-record` | 无 |
| his-system | `backend/java/his-system` | 无 |

- [ ] **ENT-P0-02a** 在 `backend/java/common` 或各模块抽取 **共享** `JwtAuthenticationFilter`（复用 his-auth 逻辑）
- [ ] **ENT-P0-02b** 每服务新增 `config/SecurityConfig.java`：`/api/health`、`/api/ready` 放行；其余需 Bearer
- [ ] **ENT-P0-02c** 验证经 Gateway 带 token 访问各服务 REST 200；无 token 401
- [ ] **ENT-P0-02d** **DoD**：7 服务 `mvn spring-boot:run` 后 Controller 可访问（非默认全 401）

---

##### ENT-P0-03 收紧 his-user 鉴权

**文件**：`backend/java/his-user/.../SecurityConfig.java`

- [ ] **ENT-P0-03a** `GET /api/departments/**`、`GET /api/patients/**` 是否公开：按产品定（演示可只读公开）
- [ ] **ENT-P0-03b** `POST/PUT/DELETE` patients、employees、departments **必须** JWT
- [ ] **ENT-P0-03c** 网关直连 8082 绕过场景：生产 NetworkPolicy 仅允许 Gateway → 微服务
- [ ] **ENT-P0-03d** **DoD**：匿名 POST 患者返回 401；admin token 可写

---

##### ENT-P0-04 JWT 密钥外部化并统一

| 位置 | 当前问题 |
|------|----------|
| `backend/go/cmd/gateway/main.go` | 硬编码 `InitWithHMAC("...-secure")` |
| `AuthService.java` | 默认 `...-secure` |
| `JwtAuthenticationFilter.java` | 默认 `...-2026` **不一致** |
| `docker/k8s/base/secrets.yaml` | 无 JWT 项 |

- [ ] **ENT-P0-04a** 统一 env 名：`JWT_SECRET`（≥32 字符 HS256）
- [ ] **ENT-P0-04b** Gateway 从 env 读取；删除源码默认密钥
- [ ] **ENT-P0-04c** Java auth + filter + 各服务 filter 同一 `@Value("${jwt.secret}")`
- [ ] **ENT-P0-04d** `docker/.env.example`、`k8s Secret` 模板增加 `JWT_SECRET`
- [ ] **ENT-P0-04e** 编写轮换说明（双密钥过渡期，可选）
- [ ] **ENT-P0-04f** **DoD**：改 env 后 admin 登录 token 可被 Gateway + 各 Java 服务验过

---

##### ENT-P0-05 Gateway 路径与后端对齐

**根因**：`handler/proxy.go` 原样转发 `req.URL.Path`。

**已知不一致（需 rewrite 或改 Controller）**：

| 前端/Gateway 请求 | 后端实际 `@RequestMapping` | 影响 |
|-------------------|---------------------------|------|
| `/api/departments` | `/api/departments`（his-user） | Gateway 仅注册 `/api/user/departments` |
| `/api/user/patients` | `/api/patients` | 管理端 `patient.ts` |
| `/api/clinic/encounters` | `/api/encounters` | 门诊 |
| `/api/prescription/prescriptions` | `/api/prescriptions` | 处方 |
| `/api/health-record/patients/:id/overview` | `/api/health-record/overview/:id` | 健康档案 |

- [ ] **ENT-P0-05a** 方案选型：**A** Gateway `Director` 增加 path rewrite 表；**B** 统一 Java/Go Controller 前缀与 `router.go`（二选一，文档化）
- [ ] **ENT-P0-05b** 在 `router.go` 增加顶层 `/api/departments` → user 代理（或 rewrite）
- [ ] **ENT-P0-05c** 修复 user/patients、clinic、prescription、health-record 映射
- [ ] **ENT-P0-05d** 对照 `frontend/**/api/*.ts` 与 `docs/API接口规范文档.md` 做矩阵测试
- [ ] **ENT-P0-05e** **DoD**：管理端科室树/患者列表、患者端科室列表经 **8080** 均 200

---

##### ENT-P0-06 全服务 `/api/health` + `/api/ready`

- [ ] **ENT-P0-06a** Java：每服务 `HealthController` → `GET /api/health`（liveness）、`GET /api/ready`（查 DB）
- [ ] **ENT-P0-06b** Go：各 `cmd/*/main.go` 注册 health；`ReadyHandler` 接 `database.Ping` / Redis
- [ ] **ENT-P0-06c** 废弃非标准路径或做 302：`/api/auth/health` → 文档说明过渡
- [ ] **ENT-P0-06d** 更新 `docker/k8s/base/*.yaml`：liveness→health，readiness→ready
- [ ] **ENT-P0-06e** 更新 `scripts/verify_demo.sh` 与 §1.6
- [ ] **ENT-P0-06f** **DoD**：`kubectl apply` 后 Pod 全部 Ready；DB  down 时 ready 失败、health 仍 200

---

##### ENT-P0-07 Java 单元/冒烟测试

- [ ] **ENT-P0-07a** `his-auth`：`AuthControllerTest` 登录 200/401
- [ ] **ENT-P0-07b** `his-user`：`PatientControllerTest` 列表分页
- [ ] **ENT-P0-07c** 其余 7 服务：至少 `@SpringBootTest` contextLoads
- [ ] **ENT-P0-07d** 去掉 `docker/Dockerfile.java` 中 `-DskipTests`（或 CI 单独 test stage）
- [ ] **ENT-P0-07e** **DoD**：`mvn test` 全模块通过；覆盖率报告可选

---

##### ENT-P0-08 K8s Secret 治理

- [ ] **ENT-P0-08a** 从 git 移除 `secrets.yaml` 中真实/弱口令，改为 `secrets.yaml.example`
- [ ] **ENT-P0-08b** 对齐 ConfigMap 中 RabbitMQ URL 密码与 Secret
- [ ] **ENT-P0-08c** 文档化：`kubectl create secret generic his-secrets --from-env-file=...`
- [ ] **ENT-P0-08d** **DoD**：仓库无可用凭据；部署文档可复现

---

#### 10.9.3 ENT-P1 — staging 前重要项

##### ENT-P1-01 可观测性（日志 / Metrics / Trace）

- [ ] **ENT-P1-01a** Go 全服务改用 `pkg/logger`（Zap JSON），替换 `log.Printf`
- [ ] **ENT-P1-01b** Gateway + Go 服务暴露 `GET /metrics`（prometheus client）
- [ ] **ENT-P1-01c** Java 引入 Spring Actuator：`/actuator/health`、`/actuator/prometheus`（按需暴露）
- [ ] **ENT-P1-01d** 请求链路透传 `X-Request-Id` 至下游（proxy 已部分支持，补全 Java filter）
- [ ] **ENT-P1-01e** （可选）OTel SDK + Jaeger 本地 compose 样例
- [ ] **ENT-P1-01f** **DoD**：Grafana 或 curl metrics 可见 QPS/延迟骨架

##### ENT-P1-02 Readiness 与 K8s 探针

- [ ] **ENT-P1-02a** 实现 §ENT-P0-06 后，K8s readiness 仅指向 `/api/ready`
- [ ] **ENT-P1-02b** 滚动发布：ready 失败 Pod 不接流量
- [ ] **ENT-P1-02c** **DoD**：人为停 PostgreSQL 后 ready 失败、Pod 不 Ready

##### ENT-P1-03 Java 统一 ApiResponse

- [ ] **ENT-P1-03a** `common` 模块新增 `ApiResponse<T>`（code/message/data/timestamp）
- [ ] **ENT-P1-03b** 改造 auth/user 两个服务 Controller（试点）
- [ ] **ENT-P1-03c** 错误码对齐 `backend/go/pkg/errors/errors.go` 与 API 规范文档
- [ ] **ENT-P1-03d** **DoD**：登录/患者列表 JSON 形状与 Go Gateway 响应一致

##### ENT-P1-04 OpenAPI / Swagger

- [ ] **ENT-P1-04a** Java：`springdoc-openapi-starter-webmvc-ui`，每服务 `/v3/api-docs`
- [ ] **ENT-P1-04b** 聚合：Gateway 路由表导出或 BFF 聚合文档
- [ ] **ENT-P1-04c** CI  artifact：`docs/openapi/openapi.yaml`
- [ ] **ENT-P1-04d** **DoD**：Swagger UI 可试调 login + departments

##### ENT-P1-05 API 版本 `/api/v1`

- [ ] **ENT-P1-05a** Gateway 增加 `/api/v1/*` 路由组（与 v0 并存过渡期）
- [ ] **ENT-P1-05b** 前端 env 切换 `VITE_API_PREFIX=/api/v1`
- [ ] **ENT-P1-05c** 文档标注废弃时间表
- [ ] **ENT-P1-05d** **DoD**：v1 与现有路径双通至少一个版本周期

##### ENT-P1-06 CORS 白名单

- [ ] **ENT-P1-06a** `CORS_ALLOWED_ORIGINS` env（逗号分隔：`http://localhost:5174,5175`）
- [ ] **ENT-P1-06b** 生产禁止 `*`
- [ ] **ENT-P1-06c** **DoD**：非法 Origin 预检失败

##### ENT-P1-07 入参校验

- [ ] **ENT-P1-07a** Java DTO 加 `@NotBlank`/`@Email` 等 + `@Valid`
- [ ] **ENT-P1-07b** Go 引入 `validator/v10` 标签，绑定失败返回 2001 段错误码
- [ ] **ENT-P1-07c** **DoD**：空用户名登录返回 400 + 规范 message

##### ENT-P1-08 集成 / E2E / 前端测试

- [ ] **ENT-P1-08a** 扩展 `testing/api/`：registration 挂号、schedule 号源
- [ ] **ENT-P1-08b** Compose profile `test` 一键起栈跑 `testing/run.sh`
- [ ] **ENT-P1-08c** 前端 Vitest：authStore、API mock
- [ ] **ENT-P1-08d** Playwright：admin 登录 + 患者挂号 happy path
- [ ] **ENT-P1-08e** **DoD**：CI nightly 跑集成 + E2E（可选）

##### ENT-P1-09 代码质量工具链

- [ ] **ENT-P1-09a** 新增 `.golangci.yml`，CI 跑 golangci-lint
- [ ] **ENT-P1-09b** 补 `scripts/check.ps1`（Windows gofmt/vet/build）
- [ ] **ENT-P1-09c** Java：parent POM 接入 Checkstyle 或 SpotBugs
- [ ] **ENT-P1-09d** `.pre-commit-config.yaml`：gofmt、eslint、禁止提交 `.env`
- [ ] **ENT-P1-09e** **DoD**：本地 pre-commit 与 CI 规则一致

##### ENT-P1-10 配置 12-factor 化

- [ ] **ENT-P1-10a** Java：`spring.datasource.*` 全改 `${DB_HOST:localhost}` 等
- [ ] **ENT-P1-10b** Go：`configs/*.yaml` 支持 env 覆盖（viper AutomaticEnv）
- [ ] **ENT-P1-10c** 删除 yaml 中明文密码；仅 `.env.example` 占位
- [ ] **ENT-P1-10d** **DoD**：仅改 env 即可换库连接，无需改文件

##### ENT-P1-11 Nacos（或文档降级）

- [ ] **ENT-P1-11a** 选型：实现 Go Nacos 注册 **或** README/架构文档标注「未实现」
- [ ] **ENT-P1-11b** 若实现：Gateway 从 Nacos 拉取 upstream 列表
- [ ] **ENT-P1-11c** **DoD**：动态上下线一个 registration 实例，Gateway 可发现

##### ENT-P1-12 RabbitMQ Consumer

- [ ] **ENT-P1-12a** 定义 1 条跨服务事件（如「挂号成功 → 通知」）
- [ ] **ENT-P1-12b** Go/Java 各 1 个 Consumer + 幂等键（outbox 或 message_id）
- [ ] **ENT-P1-12c** 失败重试 + DLQ 配置（compose rabbitmq）
- [ ] **ENT-P1-12d** **DoD**：挂号后异步收到通知记录/日志

---

#### 10.9.4 ENT-P2 — 生产 polish / 规模化

##### ENT-P2-01 Gateway 弹性

- [ ] **ENT-P2-01a** RateLimiter 改 Redis 滑动窗口（多 Gateway 实例共享）
- [ ] **ENT-P2-01b** 下游代理加 Circuit Breaker（如 `sony/gobreaker`），打开时返回 503 + 统一错误码
- [ ] **ENT-P2-01c** 可配置重试（仅幂等 GET/idempotent）
- [ ] **ENT-P2-01d** upstream 多地址轮询（配合 Nacos 或静态列表）
- [ ] **ENT-P2-01e** **DoD**：停掉 registration 后 Gateway 快速失败，不拖垮线程

##### ENT-P2-02 Docker 镜像加固

- [ ] **ENT-P2-02a** Dockerfile 增加非 root `USER 65532`
- [ ] **ENT-P2-02b** `HEALTHCHECK CMD curl -f http://localhost:8080/health`
- [ ] **ENT-P2-02c** 多阶段镜像体积优化（`-ldflags -s -w` 已有则复核）
- [ ] **ENT-P2-02d** **DoD**：`docker scout` 或 trivy 无 Critical（基线）

##### ENT-P2-03 K8s 生产 overlay

- [ ] **ENT-P2-03a** `docker/k8s/overlays/prod`：Ingress + TLS
- [ ] **ENT-P2-03b** HPA：Gateway、registration CPU 阈值
- [ ] **ENT-P2-03c** PDB：`minAvailable: 1`
- [ ] **ENT-P2-03d** NetworkPolicy：仅 Ingress→Gateway→微服务
- [ ] **ENT-P2-03e** 镜像 tag 策略：`git sha`，禁止 `latest`
- [ ] **ENT-P2-03f** **DoD**：minikube/kind 上 prod overlay 部署成功

##### ENT-P2-04 生产日志与 PII

- [ ] **ENT-P2-04a** 关闭 prod `StdOutImpl` SQL 打印
- [ ] **ENT-P2-04b** 日志脱敏：手机号/身份证中间位打码
- [ ] **ENT-P2-04c** 审计关键操作写 `his_system.audit_logs`
- [ ] **ENT-P2-04d** **DoD**：生产配置下日志无完整 password_hash

##### ENT-P2-05 文档与实现一致

- [ ] **ENT-P2-05a** 新增 `docs/PRODUCTION_CHECKLIST.md`（功能 vs 规划对照表）
- [ ] **ENT-P2-05b** README 完成度表与本文 §一、§10.1 同步
- [ ] **ENT-P2-05c** 标注：RS256、Seata、Redis 限流、Nacos Go 当前状态
- [ ] **ENT-P2-05d** **DoD**：新人读 README 不会误以为功能已全部实现

##### ENT-P2-06 Seata / 分布式事务

- [ ] **ENT-P2-06a** 梳理跨库场景（挂号+扣费+库存）
- [ ] **ENT-P2-06b** 选型 Seata AT **或** 本地消息表 + 最终一致
- [ ] **ENT-P2-06c** POC 一条链路
- [ ] **ENT-P2-06d** **DoD**：文档有决策记录；至少 1 条跨服务事务可演示

##### ENT-P2-07 前端工程化

- [ ] **ENT-P2-07a** 双前端加 `eslint` + `npm run lint`
- [ ] **ENT-P2-07b** CI 跑 lint + `vue-tsc --noEmit`
- [ ] **ENT-P2-07c** 环境变量：`.env.production` API 基址
- [ ] **ENT-P2-07d** **DoD**：PR 前端改动必过 lint

##### ENT-P2-08 Java 全局异常 + gRPC Health

- [ ] **ENT-P2-08a** `@ControllerAdvice` 映射业务异常 → HTTP + code
- [ ] **ENT-P2-08b** 实现 proto `Health` RPC（至少 registration、auth）
- [ ] **ENT-P2-08c** **DoD**：非法 ID 返回统一 JSON，非 HTML 500

---

#### 10.9.5 与 §10.5 P3 的映射（避免重复跟踪）

| §10.5 编号 | 企业级编号 | 说明 |
|------------|------------|------|
| P3-T1 | ENT-P0-07 | Java 单测 |
| P3-T2～T4 | ENT-P1-08、ENT-P0-01 | 集成测试、E2E、CI |
| P3-M1～M2 | ENT-P1-11 | Nacos |
| P3-M3 | ENT-P1-12 | MQ Consumer |
| P3-M4 | ENT-P2-06 | Seata |
| P3-D1～D3 | ENT-P2-02～03 | K8s/Docker 生产化 |
| P3-TECH-1 | ENT-P0-06、ENT-P1-02 | 健康检查 |
| P3-TECH-4 | ENT-P1-01 | OTel |
| P3-TECH-5～6 | ENT-P2-01 | 熔断/LB |

---

#### 10.9.6 企业级里程碑

| 里程碑 | 对应待办 | 状态 |
|--------|----------|------|
| 安全基线（密钥+鉴权+Secret） | ENT-P0-03～04、ENT-P0-08 | [ ] |
| 路由与 Gateway 契约一致 | ENT-P0-05 | [ ] |
| CI 门禁（构建+测试） | ENT-P0-01、ENT-P0-07 | [ ] |
| K8s 可 Ready 部署 | ENT-P0-06、ENT-P0-08 | [ ] |
| Staging 可观测 + API 契约 | ENT-P1-01～05 | [ ] |
| 生产级 K8s + 弹性 | ENT-P2-01～03 | [ ] |

---

## Plan B：uni-app 小程序重构（PC 作为服务端，手机运行未发布小程序）

> 适用前提：**课程/评审明确要求用 uni-app 的“未发布小程序”演示**，并认为 `his-mp-webview`（患者端 web-view 壳）不足以满足要求。

> 目标：在不改动后端微服务与 Gateway 的前提下，新增一个基于 uni-app（`mp-weixin`）的患者端小程序，实现**可演示的核心链路**：
>
> **登录 → 在线挂号 → 我的挂号 → 个人中心（退出/查看基础信息）**
>
> 并在本机/演示 PC + 手机（同一局域网）条件下跑通。

> 如果你只是为了“在微信开发者工具内演示患者端”，不需要走 Plan B 的 uni-app 重构：直接参照 `frontend/his-mp-webview/README.md` 使用 web-view 壳即可。

### B.1 目标与边界

**目标（本 Plan B 的 Definition of Done）**

- 新建 `frontend/his-uni-patient` uni-app 项目，编译目标为 `mp-weixin`
- 小程序端完成以下页面（最小集合，优先保证联调通畅）：
  - 登录页（token 保存）
  - 首页（入口卡片）
  - 在线挂号页（选择科室 → 选择医生/号源 → 提交挂号）
  - 我的挂号页（展示当日/历史挂号列表）
  - 个人中心页（显示基础信息 + 退出登录）
- 手机访问后端：通过 Gateway 的 HTTP API（PC 作为服务端）
- 文档补齐：uni-app 启动/真机调试步骤（写进 `docs/演示说明.md` 或本节）

**非目标（本阶段不做）**

- 全量页面迁移（账单/报告/随访等非核心演示页默认不做或延后）
- 上架审核、生产域名/HTTPS 正式配置（开发/演示环境下按“本地不校验/调试”处理）
- 复杂的消息订阅、推送等（演示可先不覆盖）

### B.2 关键技术选择

- **uni-app**：Vue 3 模式（或官方推荐 ts 模板）
- **组件库**：优先使用 `uni-ui`（减少 UI 返工；也可只用基础 uni 组件）
- **网络层封装**：
  - 封装 `request.ts`：统一请求、超时、loading、错误码 Toast
  - 将 `baseURL` 从环境变量注入，避免硬编码
- **鉴权**：
  - 登录成功后写入 `uni.setStorageSync('token')` 与 `userId`
  - 全局拦截（路由守卫或请求拦截）自动携带 `Authorization: Bearer <token>`

### B.3 重构范围与现有页面映射

| 功能 | H5（现有） | uni-app（新增） | 说明 |
|------|------------|------------------|------|
| 登录 | `frontend/his-web-patient/src/views/Login.vue` | `pages/auth/login.vue` | 保存 token + userId |
| 首页 | `frontend/his-web-patient/src/views/Home.vue` | `pages/home/index.vue` | 快捷入口为“挂号/我的挂号/个人中心” |
| 在线挂号 | `views/registration/Registration.vue` | `pages/registration/index.vue` | 选择科室/医生/号源 → 提交挂号 |
| 我的挂号 | `views/appointment/MyAppointments.vue` | `pages/appointment/list.vue` | 查询当前 patient 的挂号记录 |
| 个人中心 | `views/profile/Profile.vue` | `pages/profile/index.vue` | 展示基础信息 + 退出登录 |

> 迁移策略：不要“一口气重做全部 H5”。优先将**请求/响应 DTO + 页面状态流**迁过来，UI 先保持简洁但可操作。

### B.3.1 uni-app 请求路径与参数映射（从现有 H5 `src/api/*` 迁移）

> 约定：uni-app `baseURL` 设置为 `http://<PC_LAN_IP>:8080`（Gateway 端口），并在请求中使用 `/api/...` 前缀。

| 业务 | H5 API（现有） | uni-app 调用路径（等价） | 请求参数要点 |
|------|------------------|---------------------------|----------------|
| 登录 | `authApi.login()`：`POST /auth/login`（axios baseURL=`/api`） | `POST http://<PC_LAN_IP>:8080/api/auth/login` | body：`{ username, password }`；返回：`accessToken / refreshToken / userId ...` |
| 获取号源 | `scheduleApi.getSlotsByDate(departmentId,date)`：`GET /schedule/slots?departmentId=...&date=...` | `GET http://<PC_LAN_IP>:8080/api/schedule/slots?departmentId=...&date=YYYY-MM-DD` | `departmentId` 来自科室选择；`date` 选择的就诊日期 |
| 发起挂号 | `registrationApi.register()`：`POST /registration/register` | `POST http://<PC_LAN_IP>:8080/api/registration/register` | body：`{ patientId, scheduleId, cardType, visitDate, doctorId }` |
| 我的挂号列表 | `registrationApi.getRegistrations()`：`GET /registration/list?patientId=...&page=...&pageSize=...` | `GET http://<PC_LAN_IP>:8080/api/registration/list?patientId=...&page=1&pageSize=10` | `patientId` 来自登录后的 `userId`；page/pageSize 默认第一页即可 |
| 取消挂号 | `registrationApi.cancelRegistration(id)`：`PUT /registration/{id}/cancel` | `PUT http://<PC_LAN_IP>:8080/api/registration/{id}/cancel` | `{ id }` 为 registration 记录 id |
| 个人信息（个人中心） | `authApi.getUserInfo(userId)`：`GET /users/{userId}` | `GET http://<PC_LAN_IP>:8080/api/users/{userId}` | 用登录后的 `userId` 拉取基础信息 |
| 科室列表（两步走） | 当前 H5 `Registration.vue`：科室为前端硬编码 | **Step1（快速可演示）**：仍硬编码 10 科室，与 `backend/go/sql/demo_data.sql` 对齐 | Step1：保证演示可跑通 |
|  | `his-web-admin`：`departmentApi.getList()`：`GET /departments`（可作为后续替换） | `GET http://<PC_LAN_IP>:8080/api/departments` | Step2：替换 hardcode（按 status=1、level 取根/一层） |

> 小心点：`MyAppointments.vue` 在现有仓库里对 `registrationApi.getRegistrations` 的参数传递存在不一致风险。uni-app 重构时建议统一实现一个包装函数：`getRegistrations(patientId, page=1, pageSize=10)`，避免对象/位置参数混用导致请求参数错误。

### B.4 迭代实施步骤（建议 A-E 五阶段）

#### 阶段 A：项目初始化与基础能力（0.5～1 人日）

- 创建 `frontend/his-uni-patient`
- 配置：
  - `manifest`（微信小程序基础信息）
  - `pages.json`（注册上述 4～5 页）
  - `request.ts`（统一请求封装）
  - `.env.development`（设置 `VITE_API_BASE=http://<PC_LAN_IP>:8080`）
- 实现最小鉴权存储：
  - `token`、`userId` 写入/读取工具方法

#### 阶段 B：认证与路由守卫（约 1 人日）

- 实现登录页：
  - 调用 `POST /api/auth/login`（沿用后端网关接口）
  - 成功后跳转首页
- 实现登录状态校验：
  - 页面进入时若无 token 则跳转登录页
  - token 过期/401 时清理 Storage 并回到登录页

#### 阶段 C：在线挂号联调（约 2 人日）

- 实现 `registration/index.vue`：
  - 科室列表：优先走后端科室 API（避免硬编码）
  - 号源列表：按日期 + 科室/医生拉取 schedule slots
  - 提交挂号：调用 registration API，成功后进入“我的挂号”
- 处理关键交互状态：
  - loading、错误提示、按钮禁用
  - 返回值中序号/排队号在页面展示

#### 阶段 D：我的挂号与个人中心（约 1 人日）

- 我的挂号页：
  - 调用挂号列表/详情 API（按现有 H5 的接口习惯迁移）
  - 展示状态标签（已完成/等待等）
- 个人中心页：
  - 展示姓名/电话/性别等基础信息（调用 user/patient profile API）
  - 退出登录清理 storage 并回到登录页

#### 阶段 E：微信开发者工具真机调试 + 文档补齐（0.5～1 人日）

- 手机与 PC 同一局域网
- 在微信开发者工具中：
  - 编译 `mp-weixin`
  - 真机调试运行，确保网络请求到 `http://<PC_LAN_IP>:8080/api/...`
- 若触发域名/安全限制：
  - 优先使用工具提供的开发调试设置（开发环境不校验）
  - 必要时再引入后续 Plan（内网穿透/HTTPS）作为补救
- 更新文档：
  - `docs/演示说明.md` 增加 uni-app 的启动与调试步骤

### B.5 人力预估与排期

| 阶段 | 内容 | 预计工时 |
|------|------|----------|
| A | 初始化 + request + token | 0.5～1 人日 |
| B | 登录 + 路由守卫 | 1 人日 |
| C | 在线挂号联调 | 2 人日 |
| D | 我的挂号 + 个人中心 | 1 人日 |
| E | 真机调试 + 文档 | 0.5～1 人日 |

**合计**：约 **5～6 人日**（只保证核心演示链路）。

### B.6 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| HTTP/域名限制导致真机调试失败 | 手机端无法请求 API | 开发工具优先“本地不校验”；必要时引入 HTTPS 隧道（后续计划） |
| 页面迁移过大导致延期 | 演示验收失败 | 严格控制范围：只做 4～5 页，UI 简化优先保证链路 |
| uni-app 的组件/布局差异 | UI 不一致或出现空白 | 先做功能通，再做样式微调 |
| API 形态在 uni-app 与 H5 间差异 | 请求/响应适配成本 | 复用现有 H5 的 API 模型与字段命名，必要时做 DTO 转换 |

### B.7 触发 Plan B 的“启动条件”

当满足以下任意条件时再启用本 Plan B：

- 课程/评审明确要求“uni-app 小程序形态演示”（且 web-view 壳不被接受）
- 当前可演示路径因“演示口径”受阻（例如必须在微信小程序运行环境中展示）
- 你们有至少 1 人可以连续投入约 1 周完成核心链路迁移
