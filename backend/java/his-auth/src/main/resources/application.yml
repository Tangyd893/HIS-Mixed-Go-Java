server:
  port: 8081
spring:
  application:
    name: his-auth
  datasource:
    url: jdbc:postgresql://localhost:5432/his_auth
    username: his_admin
    password: change_me_123
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
